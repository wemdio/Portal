# Insights Log

Append-only chronology. Newest first.

Format: `YYYY-MM-DD: <one-line summary>. Details: [link to page or analysis]`

---

## 2026-07-06

- **`project_contacts_history.period_id` не писался НИКОГДА (0/6251) — дневные снапшоты 9 проектов с активным периодом молча терялись с момента создания периода.** Причина: снапшоттер (`projectContactsSync.ts`) апсертил period-строки с `onConflict 'period_id,recorded_at'`, а уникальный индекс был ЧАСТИЧНЫМ (`WHERE period_id IS NOT NULL`) — PostgREST не передаёт WHERE-предикат в `ON CONFLICT`, Postgres отвечал 42P10, ошибка логировалась и глоталась. Урок: **partial unique index не работает как arbiter PostgREST-upsert'а.** Фикс двухслойный: (1) индекс заменён на полный уникальный `(period_id, recorded_at)` — уже задеплоенный воркер начал писать сразу; (2) код переведён на единый upsert по реальному инварианту `(project_id, recorded_at)`, period_id теперь атрибут строки, не ключ конфликта. Бэкфилл period_id — только там, где шкала значений совпадает со шкалой периода (baseline-вычитание, как в самом синке): 64c9 «Аутрич, Перфоманс» 23 точки (+323/день), c136 17 точек (плоские 182 — проект заморожен), 82ed 3 точки (1095−baseline 1095→0). У 2c56/d2dc/ff56/d5f9 старые строки — суммы ДРУГИХ наборов кампаний без вычета baseline (2705 против 982 у периода и т.п.) — НЕ тегать; их периоды накапливают точки с 2026-07-07. `v_portal_project_pace` для периодных проектов ожил (было points=0 у всех). Гоча версий зеркала: main-версия `sync-portal-mirror.mjs` ещё тянет `client_email`, который аудит 06.07 (e15273150, Sergey) сознательно удалил из `portal_forwarded_leads` (утечка handoff_email проекта) — прогон main-скрипта против аудированного датасета падает на этой колонке, а «починка» через ADD COLUMN возвращает утечку (случилось, откачено: колонка снова удалена, зеркало перегнано Sergey-версией). Уходит само после мержа Sergey→main. Детали — в [portal-data.md](../app/scripts/instantly-dataset/wiki/portal-data.md).

## 2026-06-12

- **Опорные цифры для экономики портала (вопрос директора про регулярные платежи).** Объём workspace: отправка **~520–600k писем/мес** (март 518k, апр 598k, май 562k; июнь на середине 274k — тот же темп). Ящики: **1,032 active + 248 connection_error, 0 managed/DFY** (~26 писем/день на активный ящик — норма 20–30). Залив новых лидов: апр 51k, май **115k** — т.е. лимит активных контактов Light Speed (100k) уже не вмещает месячный залив. Следствия для экономики: (1) текущий объём — уровень Light Speed ($358/мес) впритык/выше; (2) главная переменная при росте числа клиентов — не подписка Instantly, а **почтовые ящики** (~544 письма/ящик/мес → ~15–20 ящиков на клиента при 10k писем/мес); (3) connection_error = 19% ящиков — потенциально платим за неработающие. Запросы — в query_log `cc-2026-06-12-portal-economics`.

## 2026-06-15

- **✅ Бэкфилл исходов ЗАВЕРШЁН — канон-покрытие 99%.** Все 50 батчей (~40K судейских ответов) размечены агентами Claude волнами по 5 (волны теряли по 0-3 батча на session-лимит/429/64K-overflow — добирал автоматически; 035 вышел частичным 297→добит, 041 переполнил вывод→пофикшен chunked-режимом + loader regex `out-\d+[a-z]?`). Итого `reply_outcome_labels` = **47,708** (claude 31,835 + rules 15,873). `v_reply_outcomes`: **48,096/48,627 = 99%** размечено, позитивных 8,959. Распределение по батчам стабильное: ~12% interested, ~11% referral, ~11% question, ~22% auto_reply. Прод: ежедневную дозапись новых ответов вешать на дешёвый ключ студии (OpenRouter, как лид-квалификатор) — ~$1/мес, не на сессию Claude. Карточки/сводки теперь считают lead rate почти на полном покрытии.

## 2026-06-12

- **Бэкфилл исходов (rules + ручная разметка Claude) → покрытие меток демо-кампаний 25-30% → 67-88%; карточки заработали по-настоящему.** API внешних LLM НЕ используем (правило клиента — удалён gpt-4o-mini пайплайн). Разметка: (1) детерминированные правила `label-replies-rules.mjs` (откалиброваны по ~180 живым примерам: NDR, тикеты, OON с диапазонами дат, смена адреса, отписки, «не туда», отказы) → **15,873 пар**; (2) ручная разметка Claude по хэшу (устойчиво к дрейфу) для судейских случаев (interested/referral/question/objection) → **97 пар**. Итого 15,970 (~33% всех 48K reply-пар). `v_reply_outcomes` (013): positive = LLM(interested|referral) ∪ Instantly i_status. `v_campaign_health.reps` переключён на него (012). Метки `reply_outcome_labels` — в БД, не в git.
- **Разметка агентами Claude ПОШЛА (волнами по 5 батчей × 800).** Урок про лимит: 15 агентов разом (~2.7M токенов) бьют session-лимит и мрут все; **5 агентов (~1.4M) проходят за ~50 мин**. Волна 1 (батчи 001-005, 4000 свежих ответов): 0 провалов, файлы по 800, загружено +3036 (964 совпали с правилами). Распределение агентов: ~12% interested, ~9% referral, ~30% auto. Итого меток **19,006** (rules 15,873 + claude 3,133); канон-покрытие `v_reply_outcomes` **33% → 66%** (32,209/48,608), позитивных 8,921. Прод: ежедневную дозапись новых ответов вешать на дешёвый ключ студии (OpenRouter, как лид-квалификатор), не на сессию. **Урок: позиционная строка кодов «метка на строку» сбивается (212≠180) — размечать только key-keyed (по хэшу/lead_id).** Демо-результаты — в [analyses/2026-06-11-dataset-objectivity-audit.md](./analyses/2026-06-11-dataset-objectivity-audit.md).
- **Карточки фичи campaign-insights показаны на реальных данных (вывод в UI пока НЕ делаем — по просьбе).** `campaign-health.mjs <id>` и `client-insights.mjs <client>`. Пример (Mailganer, 7 кампаний): кампания «7.score» lead rate 22.8% [17.6-29.0] + **A/B-победитель темы** (grade A, причинный: «вопрос по рассылкам» 3.5% vs «отделу маркетинга» 1.2%, p=0.001); кампания «4.запуск» lead rate 3.0% + **bounce 12.2%** (grade B). Сводка клиента: 1141 ответивших, 124 позитивных, peer-бенчмарк к сегменту. Все цифры с Wilson CI, гейтами (sent≥200/labeled≥20), грейдами доказательности и отказом при нехватке данных.

## 2026-06-11

- **Канонический слой метрик (миграция 012) + прототип карточки здоровья кампании.** Применено в БД: `wilson_ci_low/high()`, `v_reply_facts` (1 строка на campaign×lead — единственный легальный источник «сколько ответили»; дедуп автоответчиков, кламп дат, фильтр сентинелей), `v_campaign_daily_canonical` (дневной ряд, свежайший снапшот побеждает, история с 2025-02), `v_campaign_health` (universe-тег email/snapshot_only/none, left_truncated флаг, rate-колонки NULL ниже гейтов sent≥200 / labeled≥20 — отказ вместо вранья). Прототип фичи: `campaign-health.mjs <id> | --top N` — карточка с Wilson CI, 12-недельным спарклайном, детекторами (bounce>5%, остановка отправки, тренд 7д-vs-21д через 2-prop z при ≥500/окно, A/B тем ≥1000/вариант p<0.01 — единственная зона причинного языка) и грейдами доказательности A/B/—. Первый же прогон --top 3 нашёл реальное: у двух активных Mailganer-кампаний bounce 12.2% и 9.9%. Universe-разрез: 828 email / 1,198 snapshot_only / 9 none.

- **🎉 ДНЕВНАЯ АНАЛИТИКА ХРАНИТСЯ У INSTANTLY 16 МЕСЯЦЕВ — забрали всю: 61,172 дневных строк, 4.69M отправок, с 2025-02-07.** Ретеншен ~90 дней касается только ПИСЕМ-ОБЪЕКТОВ (тел); дневные агрегаты sent/replies/opens/clicks по кампаниям живут с фев-2025. Бэкфилл `backfill-daily-analytics.mjs` прошёл по всем 2,028 кампаниям (2,008 с данными), snapshot `6455a14c`. Январь-2026, нулевой по письмам, в дневной аналитике = 207,390 отправок / 2,766 ответов / 560 кампаний. Теперь есть: сезонность, тренды, динамика любой кампании по дням за 16 мес. Тел писем за старый период по-прежнему нет (и не будет). Помесячный профиль и SQL — в [analyses/2026-06-11-dataset-objectivity-audit.md](./analyses/2026-06-11-dataset-objectivity-audit.md).
- **sync.mjs: еженедельный FULL-снапшот аналитики + быстрый темп.** По воскресеньям (UTC) ночной синк теперь снапшотит overview+steps+daily ПО ВСЕМ кампаниям (не только активным) — закрывает «85% заморожены на 05-21». Аналитика качается на 60 rpm (отдельный от /emails лимит), /emails — по-прежнему 10 rpm. Dry-run прошёл (`--dry --full --limit-campaigns=2`, snapshot=null, записей 0). Цепочки писем (последовательности/варианты с темами и текстами) хранятся в `raw_campaign_steps` для 2,016/2,028 кампаний — это конфиг кампании, Instantly его НЕ стирает, выгружен полностью.

- **Проверка «может мы неправильно скачали?» → скачали ПРАВИЛЬНО, дыра вендорская; +8,645 строк восстановлено из апрельского кэша.** (1) Помесячная матрица майского кэша (`.tmp/instantly-cache/emails/`, 1,881 файлов, 1,715,907 писем) **совпадает с БД 1-в-1** — загрузка была полной, бага нет. (2) Ретеншен ~90 дней доказан сравнением двух пулов: `emails-by-campaign.OLD-2026-04.json` (пул 22.04, 10 кампаний) **ещё содержал январь** (2,777 sent), майский пул (24.05) — уже 0 по всем 1881 кампаниям. Вендор удаляет на своей стороне (поддержка подтверждает). (3) Из апрельского кэша догружено в raw_emails **8,645 строк** (янв ue1=2,777; фев ue1=5,855; апр 13) по 4 кампаниям: Визардия.2 (4,833), SalesAi_СБИС (2,919), ALP-сбис (880), Mailganer-тендер (13) — mapping 1:1 c load.mjs, ON CONFLICT DO NOTHING. Январь в БД теперь: 2,777 (только эти кампании!), февраль: 115,114. (4) «Дополнить за полгода» из живого API невозможно: сегодняшнее окно ≈ 13 мар+, у нас непрерывно с 23 фев — БД уже богаче API. Старые данные существуют только в наших собственных кэшах (исчерпаны) и lifetime-агрегатах overview. Следствие: ночной sync — единственный хранитель истории; weekly full snapshot критичен.

- **Аудит объективности датасета (6 агентов, перед фичей campaign-insights).** Ключевое: (1) **sent-горизонт фактически ~90 дней полного объёма**, не ~6 мес: 2026-01=**0** sent-строк (при 4,229 ответах в январе → отправки точно были, но стёрты вендором), 2026-02=21% месяца, дек'25 — слайвер 7 кампаний; ~~«~6 мес гранулярно» (запись 2026-05-31)~~ → полные sent-метрики есть только с **2026-03**. (2) **844 из 1,666 кампаний с письмами — «зомби»**: ответы есть (ретеншен ~10 мес), отправки стёрты → наивный reply rate = ∞. (3) **20.2% ue2-строк — дубли автоответчиков** (до 12x на лида, НЕ баг синка) → «ответили» считать только DISTINCT (campaign_id, lead_id). (4) **Метки исходов у 30–38% ответов**; `raw_leads.interest_*` = 100% NULL; квалификатор из локального .env = 4 строки (проверить прод!); 1,513 ответов с i_status вне lookup теряются в join. (5) **Open rate непригоден для скоринга** (46% кампаний 60–100% opens — боты; 94 без трекинга). (6) **85% кампаний заморожены на единственном full-снапшоте 05-21** → нужен еженедельный full re-snap, иначе тренды слепы. (7) Мины: TZ сессии UTC+4, 2 ответа с датой 2008, steps 0-indexed (`step_n=1` → 156 вместо 1,414 A/B-кампаний), raw_emails=2.12M (док «1.7M» устарел). Гейты будущей фичи + приоритетные фиксы: [analyses/2026-06-11-dataset-objectivity-audit.md](./analyses/2026-06-11-dataset-objectivity-audit.md).

## 2026-06-05

- **Research brief pass по top Instantly questions:** после baseline pooled-среза добавлен объективирующий слой: first-email variants, min sent, impossible-open filter, campaign-centered deltas, matched within-campaign comparison, latest overview snapshot per campaign. Сильные направления: short body `<50` words, 1-5 sentences, company relevance, navigation ask. Негативные: meeting/demo/15-min CTA, sales-pitch words, case/metric claims, timeline hooks, invite/webinar/discount. Follow-ups: 2 шага лучше 1; 3-5 всё ещё добавляют reply/lead, 6+ sparse. Детали: [analyses/2026-06-05-instantly-top-research-questions.md](./analyses/2026-06-05-instantly-top-research-questions.md).

## 2026-06-04

- **Для «недавно добавленных лидов / за последние сутки» НЕ использовать `instantly_dataset.raw_leads` — он заморожен на снапшоте** (`max(pulled_at)`=22 мая; ночной `sync.mjs` обновляет только аналитику, лиды не ре-синкает). Живой источник свежих лидов — **`instantly` DB** (`instantly-postgres-prod`, db `instantly` на 144): `client_campaign_leads` (поля `email/first_name/last_name/company_name/campaign_id`; ⚠️ `synced_at` = время СИНКА, не добавления — см. правку ниже) + `client_campaign_launches` (ручные запуски клиента: `uploaded/accepted/skipped_rows`, `created_at`). Джойн к каталогу: `instantly_campaign_catalog.id::text = client_campaign_leads.campaign_id` (uuid vs text — нужен каст).
- **Проверка добора 04.06 — ИСПРАВЛЕНО (ранее ошибочно: «1992 новых за сутки, все на main, Mailganer=0»).** На деле 1992 = `client_campaign_leads.synced_at`, а mirror-синк `clientLeadsSync.ts` делает `upsert synced_at=now()` для ВСЕХ лидов активных кампаний каждый час → `synced_at` = время синка, НЕ добавления (старые даты в распределении = кампании, выпавшие из `client_instantly_access`). **Реальный выхлоп добора — `client_auto_pipeline_runs.routed_count` (main DB).** Добор (`/api/cron/auto-hh-pipeline`) бежит только для `client_auto_pipeline_configs.enabled=true` → это ТОЛЬКО Mailganer (`0a6d90e1`, account-2). Факт: прогон **02.06 completed live → routed=2551** (dur 2ч44м, HH 539 + BoB 3687); прогон **03.06 ещё `running`** (1ч32м из ~2ч44м; счётчики пишутся в конце, поэтому нули в процессе — норма). Кампании account-2 (Топ/Высокий/Средний скор) держат **9159** лидов (6982/1335/842), активны (~1681 sent, 27 ответов). **Mailganer в `client_campaign_leads`=0 — sync-gap** (его account-2 кампании не зеркалятся в портал, хотя в Instantly лиды есть). Урок: `client_campaign_leads.synced_at` ≠ время добавления; для добора смотреть `client_auto_pipeline_runs`.

## 2026-06 (Instantly support)

- **Официальные сроки хранения Instantly (подтверждено поддержкой).** Отправленные письма + тела — «ограниченный период» (числа не дают; наш замер ≈6 мес). Ответ в папке Others — **30 дней**. Ответ, **привязанный к лиду** (Primary) — **≥1 год**. Это объясняет асимметрию: ответы с авг'25, отправки с дек'25. Старше — стёрто, не вернуть (вендор подтвердил). Запись в [dataset-schema.md](./concepts/dataset-schema.md). Защита: ночной sync + привязка ответов к лидам продлевает хранение в Instantly.

## 2026-05-31

- **Горизонт датасета: ~6 мес гранулярно, расширить назад НЕЛЬЗЯ (3 метания за день, решено ИЗМЕРЕНИЕМ).** Цепочка моих ошибок на вопросе «точно нельзя больше полугода?»: (1) «стёрто, нельзя» — на дырявом тесте («пробная» кампания, не слала → ложный 0); (2) «можно, 2.63M недотянуто, дотянем до 15 мес» — принял 2.8%-хвост ProdavAI (471 из 16916) за полную доступность; (3) ИЗМЕРИЛ 40 случайных недотянутых кампаний против живого API → **восстановимо лишь 1–5% от slate** (фев–июл'25 ~0%, авг–ноя 1–3%, дек 5%). Реально вернётся ~50–130K из 2.63M. Instantly стёр основное. Итог: ~6 мес (дек'25–май'26) — почти полный гранулярный горизонт, backfill не стоит того. Для старого — только overview-агрегаты. **Урок: МЕРЬ до того как утверждать — я ошибся в ОБЕ стороны, пока не измерил.** Зафиксировано в [dataset-schema.md](./concepts/dataset-schema.md).
- **«Лиды» — операционный сигнал, не свойство датасета.** Метки `status='lead'` — выхлоп live-квалификатора портала (с 13 апр, OpenRouter, для ТГ-алертов СЕЙЧАС), покрывает 6% ответов / ~7 недель. Весь лид-анализ сессии (Чизмол/inMyRoom/НАФИ/Roistat) — на этом 7-недельном срезе, не на «годах». Датасет нативно лидов НЕ содержит; «лид=деньги» вообще в CRM, которой нет. Анализ датасета держать на том, чем владеет: reply-поведение, темы, sequences, клиенты, доставка.

## 2026-05-30

- **Adversarial workflow (22 агента, 1.8M токенов): 3 из 4 глубоких гипотез УБИТЫ скепсисом, 1 выжила.** query_log id=18-21. ✅ ВЫЖИЛА (conf 78, прошла все атаки): **глубина последовательности** — follow-up ответы конвертят в лид ~2× лучше первого касания (within-campaign 4.59% vs 10.66%, z=6.35, paired p=0.0022); шаги 2-3 несут ~47% лидов. НЕ резать последовательности. ❌ УБИТЫ (все три — конфаунд **возраста кампании**): «reply-only = мусорные ответы» (позитив-доля идентична 47.6 vs 46.5%, p=0.28); «mailbox health = рычаг на лиды» (60× градиент = age+coverage конфаунд); «малые списки бьют большие» (артефакт раздутого `contacted_count` + возраста; корреляция меняет знак). Все выводы → [playbook.md](./playbook.md). Операционный claim агента про «краш квалификатора на 412» опровергнут живыми логами (0 ошибок, здоров).
- **Дневная аналитика: purge + замена (migration 009).** `raw_campaign_analytics_daily_snap` обнулена (1.94M workspace-мусора), создан `v_campaign_daily` из `raw_emails` (корректный per-campaign дневной ряд sends/replies, без API). Баг затрагивал ТОЛЬКО эту таблицу с самого первого пула; остальной датасет корректен; ни один прошлый вывод на ней не строился.
- **`raw_campaign_analytics_daily_snap` сломана: workspace-wide ряд под каждым `campaign_id`.** Все 1881 кампаний имеют идентичный `sum(sent)=7 381 152` и byte-identical 965-дневный ряд — включая кампании с lifetime `contacted_count`=1..5. `raw_payload` содержит те же числа → не наш парсинг, API вернул workspace-данные. Это и есть «1.8M daily metrics» из 2026-05-23 (1881×965 ≈ 1.8M — один ряд, размноженный по кампаниям). **Корень:** `/campaigns/analytics/daily` фильтруется по `campaign_id`, а наш код шлёт UUID под ключом `id` (endpoint его игнорирует → отдаёт все кампании). Баг в 3 местах: [`client.ts:175`](../app/src/lib/instantly/client.ts), [`sync.mjs:436`](../app/scripts/instantly-dataset/sync.mjs), [`pull.mjs:409`](../app/scripts/instantly-dataset/pull.mjs); `syncStepAnalytics` рядом шлёт `campaign_id` правильно. Overview — корректна, ей верим. Дневной ряд кампании строй из `raw_emails`. Поправил COMMENT на таблице (migration 008) + caveat в [dataset-schema.md](./concepts/dataset-schema.md#️-raw_campaign_analytics_daily_snap-сломана-workspace-wide-ряд-под-каждым-campaign_id-2026-05-30). Фикс кода + ре-pull дневной аналитики — TODO (баг продолжается каждую ночь, sync.mjs nightly).

## 2026-05-29

- **Agent ran 3 clients (query_log id=3,4,5), then SELF-CORRECTED id=3.** Initially claimed inMyRoom succeeds because of its subject line — user challenged it. Verification (within-campaign A/B) refuted: same subject 2.74% on one list, 0.00% on another; all 36 leads came from the real-estate-agency segment, not any subject. Real driver = segment/ICP. Lesson written to [subjects/winning-patterns.md](./subjects/winning-patterns.md): subject affects only reply (inconsistently, list-dependent), never attributable to leads. ЮРКОМ («0 лидов» не баг — 171/175 not_lead, аудит-коммодити, bounce 7%) and НАФИ (winning «Исследовательский чекап» 7.45% on SMB diluted by wrong-ICP developer campaign) hold up.
- **Dropped "System A" (migration 006).** Briefly built a deterministic SQL health-calculator (`health-check.mjs` + dim tables + snapshot storage) — wrong approach, removed it. The eval loop is AI-agent-in-the-loop: the agent answers client questions on demand and logs to `query_log`; weekly we review + improve. Client→campaign map queried LIVE (no rot). KEPT the genuine wins: `v_subject_performance` fix + `v_campaign_mailboxes` (migration 005), and `sync.mjs` now refreshes step+daily analytics. See [client-health-questions.md](./concepts/client-health-questions.md).
- **Eval loop first real iteration (query_log id=2, ДПО ПРОФ).** Acting as the agent surfaced 2 dataset bugs (subjects NULL for all campaigns; mailbox health blind to tag-based senders), both fixed same session. Diagnosis that a calculator couldn't reach: ДПО's low reply is the client's base quality, not our delivery (64 mailboxes healthy, reply uniformly low across all subjects).

## 2026-05-26

- **Self-improving eval loop (migration 003).** Added `query_log` table + two views (`v_query_log_review_queue`, `v_query_log_repeats`) + concept page [eval-loop.md](./concepts/eval-loop.md) + weekly review template. Inspired by [YC talk on self-improving companies](https://www.youtube.com/watch?v=X_JsIHUfUjc) (Tom Blomfield, 4:12 — monitoring agent watching every query, identifying failures, auto-fixing overnight). Our v1 is human-in-loop weekly review; promote to autonomous when patterns stabilize. Mandatory logging now in `CLAUDE.md`.
- **Wiki initialized.** Three-layer architecture in place: SQL truth-layer → wiki summarization layer → user. AI agent now reads `CLAUDE.md` first every session. Detail: [README.md](./README.md).
- **Schema cleanup completed (migration 002).** Dropped `raw_webhooks` (operational), `raw_emails.is_unread/is_focused/body_html` (UI flags, never meaningfully populated). Added 6 `lookup_*` tables decoding Instantly's magic numbers + 74 `COMMENT ON` (23 table + 51 column) so AI introspection works. Detail: [concepts/dataset-schema.md](./concepts/dataset-schema.md).
- **Found undocumented `ue_type=4`.** Only 2 rows in 1.7M. Outbound "Re:" from our mailboxes, likely auto-follow-up. Added to `lookup_ue_types` with TODO to verify with Instantly. Source: query `SELECT count(*), array_agg(DISTINCT subject) FILTER (WHERE ue_type = 4) FROM raw_emails`.

## 2026-05-23..25

- **Initial full dataset pull.** Snapshot `98974f54-5723-4555-9ca9-20499b79cd2c`. 1,881 campaigns / 167,735 leads / 1,715,907 emails. ~~1.8M daily metrics~~ → **WRONG on two counts (corrected 2026-05-31):** (1) the "1.8M daily metrics" were the poisoned workspace-replicated `daily_snap` (see migration 009); (2) "years of history" is FALSE — granular sends only go back to **11 Dec 2025** (~5.5 mo); Instantly purged older per-email data (~1.2M old sends survive only as overview aggregates). The dataset is a ~6-month granular window, not multi-year. Total DB ~9 GB.
- **Discovered Instantly /emails rate limit ~10-15 RPM sustained** (regardless of API key — workspace-wide). 20+ RPM triggers throttling with sliding penalty. Detail: [pull-campaign-analytics.mjs](../app/scripts/pull-campaign-analytics.mjs) header comment.
- **Worker interference incident.** Running pull at 30+ RPM degraded `portal-worker-instantly-leads` poll cycle from 30s to 20+ min between 14:12-15:25 UTC on 22 May. ~2-4 missed lead qualifications. Lesson: any /emails pulling shares budget with the qualifier worker. Mitigation: `docker stop portal-worker-instantly-leads` during heavy pulls, restore after.
- **Per-V8-string-limit crash.** `JSON.stringify` of `emails-by-campaign.json` at 646 MB exceeded V8's ~512 MB string limit. Migrated cache to per-campaign files (`emails/<id>.json`) in `pull.mjs`. Won't recur. Detail: [migrate-cache-to-per-campaign-files.mjs](../app/scripts/instantly-dataset/migrate-cache-to-per-campaign-files.mjs).

## Earlier

- **AI lead qualification pipeline runs on prod** (`portal-worker-instantly-leads` container, polls every 30s, /emails ue_type=2, dedupes against `instantly_lead_qualifications`, classifies via OpenRouter, sends Telegram alert only for `status='lead'`). Self-hosted Supabase backing (NOT the cloud project in `.env` — that's stale). See [leadQualificationWorker.ts](../app/src/lib/instantly/leadQualificationWorker.ts).
2026-07-02: raw_emails НЕ содержит bounce-событий (ue_types только 1/2/3/4); per-lead bounce в raw_leads.raw_payload->>'status' есть НО синк лидов неполный (кампания с bounced_count=6224 имеет 27 лидов status=-1) — per-domain баунс-историю из датасета брать нельзя до бэкфилла. Sent-покрытие по доменам отличное: 83% catch-all доменов живой базы АДК уже в истории (622/830 с >=3 отправками). См. analyses/2026-07-02-domain-history-signal-feasibility.md

2026-07-02: OutreachOS-секвенция задизайнена на данных (workflow, 77 SQL, адверсариал-верификация): 3 шага = 93.5% ответов (58.3/23.7/11.5%), роутинг-тема «К кому обратиться в „{{companyName}}"» 4.43% на 16.6K + 4.2–5.4% на своих HH-B2B базах, тело <50 слов ВКЛЮЧАЯ подпись (счётчик датасета включает её). ВАЖНО: клейм playbook «follow-up конвертят 2×/47% лидов» ОПРОВЕРГНУТ на полном v_reply_outcomes (first-touch 20.4% vs 13.8% within-campaign) — playbook аннотирован contested; follow-up'ы оправданы объёмом (~36% ответивших). См. analyses/2026-07-02-outreachos-sequence-design.md

2026-07-02 (2): Оффер-майнинг корпуса (1428 кампаний ≥1000 sent, v_reply_outcomes): выигрывают роутинг+«поставка того, что и так закупают» (VELAR 22.9/1000), канальное «вы заработаете» (единств. живое «партнёрство»), боль=выручка ниши (TSMedia 13.6); мертвы промо-бласт/«бесплатный аудит» консультанта (НАФИ, BYTIME: 100+ ответов, 0 позитивов)/SaaS-automation-питч. Роутинг-шаг1: медиана 1.19 vs 0.54 поз./1000. Ссылка в шаге 1 режет до 2.3% (vs 10.9%) доли кампаний ≥5 поз./1000; call-ask → 4.4%. Прецедент продажи аутрича: Polza «b2b hh» 11.5 поз./1000 (роутинг → оффер-источник-клиентов+свои объёмы → прозрачность). До половины позитива роутинга = referral. Финальная секвенция OutreachOS пересобрана по этому каркасу; цифры в копи верифицированы (весна: 1 677 925 sent / 25 159 lead_reply — «1,9 млн» из черновика было завышением). См. analyses/2026-07-02-outreachos-sequence-design.md

2026-07-11: **«Призраки» в simple-list таблицах: синк никогда не удалял исчезнувшие строки.** Ночной sync.mjs переливает полные списки (custom_tags, custom_tag_mappings, accounts...) UPSERT'ом, но удалённые в Instantly строки оставались у нас навсегда; pulled_at при этом НЕ обновлялся (= «когда впервые увидели», не «когда видели в последний раз»). Обнаружено через дашборд «Нагрузка почт»: тег «Умные Новации» показывал 16 ящиков, в Instantly — 0 (удалены в мае). Сверка с живым API: **465 маппингов-призраков (19% таблицы!)** + 1 тег (РШУ_Тест); раздуты были «неименные почты» (72), Mailganer (33, реально 7!), delapr (27), App event (26). «Дубли» (tag_id,resource_id), найденные ревью 11.07 — тоже призраки (старые копии с другими id), после чистки dup_pairs=0. Разовая чистка применена (diff live API → DELETE, самовосстановление при ошибке — списки переливаются еженощно). Фикс синка: syncSimpleList теперь обновляет pulled_at (семантика «last seen») + deleteMissing для custom_tags/custom_tag_mappings (только current-state зеркала; campaigns/accounts/lead_lists НЕ чистим — на них смотрит история raw_emails/raw_leads → у аккаунтов призраки остаются, ~сотни строк). Прод-крон подхватит после deploy-sync.sh.

2026-07-14: **Призраки в raw_accounts (продолжение): 534 из 1548 почт (34%) удалены в Instantly, но жили у нас.** Найдено через дашборд: тег «ИЗИ Контроль» показывал 28 ящиков против 27 в Instantly — лишним был contact@polzasender.ru (аккаунт удалён, но привязка к тегу в /custom-tag-mappings у Instantly ОСТАЛАСЬ — их API сам не чистит мапы удалённых аккаунтов, поэтому deleteMissing по маппингам от 11.07 тут не помогает). Влияние на дашборд: +43 фантомных ящика и +3185 писем/день фантомного потолка по 12 тегам (ITLS 31→26, «неименные почты» 232→215, Alial 49→47, Польза Ру 36→33) → утилизация занижалась. ФИКС: миграция 019 — soft-delete `raw_accounts.deleted_at` (hard-delete нельзя: на email смотрит история raw_emails.eaccount, 2M+ писем). Контракт: `deleted_at IS NULL` ⇔ ящик есть в Instantly сейчас; текущее состояние (потолки/утилизация) фильтровать по нему, историю — нет. syncAccounts теперь проставляет/снимает deleted_at по живому пулу + обновляет pulled_at. Бэкфилл 534 применён, sync задеплоен на 139.

2026-07-15: **⛔ ОТКАТ подхода «удалять исчезнувшее из Instantly» (инцидент). Списочные API Instantly НЕЛЬЗЯ использовать как источник истины по составу.** Курсорная пагинация (/accounts, /custom-tag-mappings) нестабильна ВО ВРЕМЕНИ: разные ночи возвращают разное число строк (замерено 1939/1839/1739 маппингов; next_starting_after обрывает выдачу раньше без ошибки). Поэтому и deleteMissing по маппингам (11-14.07), и soft-delete аккаунтов (14.07) РАЗРУШАЛИ живые данные: у активного клиента «Зиккурат» осталось 5 привязок из 16 → потолок считался по 2 ящикам → «142% перебор». В логе прода прошлой ночи `custom_tag_mappings: 200 deleted`. Ручные чистки 11/14.07 (удаление 465 маппингов + soft-delete 534 почт) тоже частично удаляли живое. ФИКС: (1) sync.mjs → upsert-only, deleteMissing удалён полностью, аккаунты только un-delete «если снова виден» + pulled_at=last-seen; (2) восстановлены маппинги из ОБЪЕДИНЕНИЯ нескольких пулов (union устойчив, единичный пул — нет); (3) все 534 soft-delete сняты (deleted_at=NULL) → чистое накопительное состояние. Сверка 57 тегов: дефицитов НЕТ (нигде БД<Instantly), остались только over-count призраки (мягкая ошибка, безопасное направление; лучше показать лишний мёртвый ящик, чем «142%»). Колонка raw_accounts.deleted_at (мигр.019) и фильтр в mailboxLoad ОСТАВЛЕНЫ как инфра под будущую НАДЁЖНУЮ чистку по «не виден N дней» (pulled_at-stale, когда last-seen созреет). Урок: НЕ импортировать sync.mjs для проверки синтаксиса (стартует при импорте) — только `node --check`.

2026-07-15 (2): **Дашборд нагрузки: скрытие тегов Coldy/Trigga (только прогрев).** Клиенты с work_format 'Колди'/'Тригга' (portal_projects) ведутся вне Instantly, их ящики там только греются → тег вечно «Простой 0%» = ложная тревога. Автосвязь тег↔проект невозможна: у таких проектов НЕТ кампаний в Instantly, а имена тегов ≠ имена клиентов (mateca↔Маца, транслитерация; матч по имени ловит ~6/15). Решение — курируемая таблица `mailbox_load_hidden_tags(tag_id,tag_name,reason)` (мигр.020); дашборд исключает эти теги из строк И из итогов (mailboxLoad: mappings/tagged CTE + note со счётчиком). Засеяно 12: 11 Coldy (uis×2,artefact,D1,Hydromodul,Law Russia,Loya,redev,software cats,iiiitech.space,Южный регион) + mateca (Trigga). Проверено: у всех ~0 отправок за 30д (iiiitech 16=прогрев-шум). НЕ засеяны (нет warmup-тега в Instantly или имя не гуглится): DECO FORM, www.yamaguchi.ru, АДК Транс, ДАНИ — ждут ручного tag_id. Дополнять: INSERT в mailbox_load_hidden_tags (передеплой приложения не нужен, читается вживую).

2026-07-15 (3): **Уточнение по призракам: аккаунты чистить БЕЗОПАСНО, откат от 15.07(1) был избыточен.** /accounts стабилен (1014 в 9 пулах подряд) — в ОТЛИЧИЕ от /custom-tag-mappings (курсор скачет 1739/1839/1939). Сломало Зиккурат именно удаление МАППИНГОВ по усечённому пулу, а soft-delete АККАУНТОВ был корректен — я зря откатил его в панике, свалив в кучу. Возвращено правильно: (1) разовая union-чистка (объединение 3-4 пулов /accounts) пометила 534 удалённых аккаунта — проверено на конкретных тегах, что живые не тронуты (Зиккурат 16→16, Alt-Point 16→16, EventIQ 12→12), а призраки ушли (publio 4→0, хлеб 16→0, инф рассылка 1→0, ИЗИ 28→27); (2) в syncAccounts возвращён soft-delete СО СТРАХОВКОЙ: помечать удалёнными только если пул покрывает >=90% текущих живых (защита от усечённого пула) + обратная разметка при возврате в пул (самолечение ложных). Маппинги по-прежнему upsert-only (их курсор нельзя). publio/хлеб/инф рассылка = теги с 0 живых ящиков → на дашборде больше не показываются. Alt-Point/EventIQ (прогрев перед запуском) остаются — это НЕ Coldy/Trigga.
