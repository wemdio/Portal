# VE2: cheaper classification with admission confirmation

User approved switching collection models and the Sergey → test → main → scheduled deployment release on 2026-09-17.

`gate` and `relevanceReview` default to `deepinfra/deepseek-v4-flash-0731`. This includes the existing auxiliary users of `gate`; research, bulk analysis and email generation retain their separate settings. Existing VE_MODEL_GATE/VE_MODEL_RELEVANCE_REVIEW overrides still work. Neither production override was set during the read-only check at01:22UTC. ENG and HE_MODEL_* are untouched.

The Requesty route supports strict JSON schemas in the paid pilot. Both native constraints and local full-batch/evidence validation are used. No reasoning_effort override: low reasoning timed out at90seconds on a real clinic batch and was rejected before release. Classification has a90second deadline and at most2HTTP attempts per schema attempt. Ordinary strict schema recovery remains bounded as before.

The cheap semantic review does not alone authorize a new positive admission. `direct_match` and a direct contradiction against the classifier reserve the existing second semantic attempt with GPT-5-mini, using identical excerpts without revealing the first verdict to that model. Positive candidates share batches of up to8; malformed retries remain isolated. Insufficient evidence does not buy an additional opinion. A repeated failure preserves the company without admission; billing/configuration failures keep their existing stop behavior. No third semantic attempt is allowed for the same evidence. Successful prior GPT checks are preserved.

Model routing is decoupled from the existing checkpoint identity for these two explicitly compatible defaults. The target, language, company facts, reader revision and evidence remain part of their old keys. Arbitrary custom model overrides remain isolated. This prevents paying again for old verdicts, website reads, citation repairs and prepared company names solely because the default model changes. This is a narrowly scoped rollout compatibility rule, not a shared cross-hypothesis verdict cache.

## Validation and measured limits

Existing lifecycle assertions cover native schemas, target guards, old→new checkpoint reuse, confirmation/resume without repeating classification, batching of positive confirmations, missing facts without a paid second opinion, malformed replies, transport errors and bounded attempts. No new test files or it() blocks. Final full suite:225suites/2473tests passed in56.14seconds. TypeScript, ESLint, worker bundle and diff checks passed.

Private reproducibility artifacts: `~/.codex/handoffs/ve2-deepinfra-eval-20260917/switch-*`, scripts `run-switch*.mjs`. Three labelled hypotheses with12company inputs passed primary admission controls; final fast reviewer passed12/12 admission controls. Six real hypotheses,48review inputs: all6fast replies complete, median16.6seconds. GPT subsequently checked10positive candidates in4batches. Combined reported cost$0.010625592 versus prior GPT-only$0.02860375 (62.85%less;2.69x). This stage-level replay excludes contradiction-driven confirmations, production retries, website/email work, Serper and prior collection costs; it is not a total production invoice claim. Warm caches affected some calls. The final production delta must include all attempts and use matched cohorts/periods.

Content inspection: fast review correctly retained the clinic network shown by explicit branches; GPT confirmation withheld a fertilizer producer without required laboratory evidence and three HoReCa candidates without required distribution/service evidence. The prior baseline also missed some mandatory conditions. A regional pharmacy chain was left insufficient by the fast review due to unclear federal scope. No independently labelled representative real dataset exists, so equal recall/accuracy and zero loss of quality are not established. Controls and evidence spot checks do not prove every collected contact correct.

Rollback is a normal tested release or an explicitly authorized environment override; no automatic global rollback or reclassification. After deployment verify the actual worker bundle, new provider model usage, health, queue/base/preparation progress, expensive confirmation fraction, errors and same-cohort cost. Monitor at06/08/10and report12:00Astrakhan on17September; user authorized follow-up fixes through the same release path if post-deploy collections fail.
