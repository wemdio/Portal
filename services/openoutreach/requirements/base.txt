playwright>=1.59
playwright-stealth
pydantic
pydantic-ai-slim[openai,anthropic,google,groq,mistral,cohere,bedrock]
# pydantic-ai 1.96 imports `from griffe import ...`, but griffe 2.x split into an
# empty `griffelib` meta-package and reorganized symbols. Pin until upstream fixes.
griffe>=1,<2
jsonpath-ng
pandas
jinja2
termcolor
Django>=5.2,<7.0
tendo
pyyaml
fastembed
scikit-learn
huggingface_hub
# linkedin_cli — Django-free LinkedIn-библиотека (Voyager client, browser/auth,
# actions, enums). ВНИМАНИЕ: имя pip-дистрибутива — `linkedin-agent-cli`, а имя
# ИМПОРТА — `linkedin_cli` (дефис→подчёркивание + дроп "agent"). Импортируется на
# top-level в crm/models/deal.py, crm/models/lead.py, миграции crm/0004 и всех
# portal_daemon-тасках → грузится при django.setup(). Без него любой manage.py
# падает с ModuleNotFoundError: No module named 'linkedin_cli'. Репо публичный,
# clone без auth (git ls-remote → ok), git есть в build-стейдже Dockerfile.
linkedin-agent-cli @ git+https://github.com/eracle/linkedin-cli.git@main

# openoutreach (git+https://github.com/eracle/openoutreach-cli.git@main) — НЕ
# возвращаем: репо upstream'ом удалён (GitHub → "Repository not found"; git в
# неинтерактивном build просит креды → "could not read Username" — это и валило
# сборку, а НЕ linkedin-cli выше). Модуль `openoutreach` нужен только lazy-import
# внутри linkedin/onboarding.py:collect_from_wizard (interactive self-host wizard),
# daemon/Docker его не вызывают. Понадобится wizard — вендорить prompts/wizard
# локально или зеркалить во внутренний git.

# Portal-native fork: Postgres backend через DATABASE_URL (см. django_settings.py).
dj-database-url>=2.1.0
psycopg[binary]>=3.1.0
asgiref>=3.7.0
# Portal-native fork: thin HTTP client для OpenRouter LLM-вызовов из daemon'a.
# Не используем openai SDK / pydantic-ai чтобы не тянуть upstream'овский pin.
httpx>=0.27.0
