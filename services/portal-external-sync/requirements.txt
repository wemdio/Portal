asyncpg==0.29.0
httpx==0.27.0
apscheduler==3.10.4
python-dotenv==1.0.1
# Тянется транзитивно через httpx, но с 27.08.2026 импортируется напрямую
# (sources/bank_tochka.py собирает свой набор корней для УЦ Минцифры).
# Намеренно без точного пина: certifi — это и есть набор доверенных корней,
# и замораживать его значит замораживать отзывы скомпрометированных УЦ.
certifi>=2024.2.2
