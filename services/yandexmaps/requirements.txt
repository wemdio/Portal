fastapi>=0.115.0
uvicorn[standard]>=0.30.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
# Playwright — живой, поддерживается Microsoft, релизы каждые 2 недели.
# Auth-прокси нативно, без mitmproxy-костыля. Chromium скачивается в
# рантайме через `playwright install` (мы этого не делаем — базовый
# образ mcr.microsoft.com/playwright/python уже содержит всё нужное).
playwright>=1.49.0
# Инжектит JS-патчи в каждую страницу, стирающие следы Playwright'а
# (navigator.webdriver, WebGL fingerprint, plugins, chrome.runtime и т.п.).
# Аналог undetected-chromedriver для Selenium.
playwright-stealth>=1.0.6,<2.0
