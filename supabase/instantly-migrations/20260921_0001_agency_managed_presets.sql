-- Агентский кабинет клиента: логин и пресет завёл специалист студии из Движка
-- вертикалей. Обязательства по такому клиенту закрывает договор студии, а не
-- его self-serve подписка, поэтому тарифные гейты запуска и долива к нему не
-- применяются. Всё остальное в силе: blocklist, доменная политика, журнал
-- отправок, пул ящиков пресета.
--
-- Флаг ставит ТОЛЬКО сервер при создании кабинета из Движка. Существующие
-- пресеты остаются false, поведение оплаченных self-serve клиентов не меняется.
alter table public.client_campaign_presets
  add column if not exists agency_managed boolean not null default false;

comment on column public.client_campaign_presets.agency_managed is
  'true — кабинет заведён студией из Движка вертикалей: тарифный гейт запуска и долива не применяется (обязательство закрывает договор студии).';
