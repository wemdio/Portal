-- Бриф для генератора персонализированных ответов больше не хранится в
-- reply_personalization_kb: к запуску кампании бриф уже заполнен в карточке
-- проекта, поэтому инструмент всегда берёт его живьём из projects.brief_text.
-- Модалка базы знаний показывает бриф проекта read-only; редактирование —
-- только в карточке проекта. Здесь остаются факты/тон/пример, которых в
-- карточке проекта нет. Таблица на проде пуста, drop column безопасен.

alter table public.reply_personalization_kb drop column if exists brief;

comment on table public.reply_personalization_kb is
  'Карточка знаний проекта для генератора персонализированных ответов на входящие: факты о продукте, тон, пример письма (бриф — не здесь, а живьём из projects.brief_text). Одна строка на проект.';
