-- VE2: широкие гипотезы уровня сектора («Частная медицина», «Оптовая торговля»)
-- для ежедневного добора. Генератор выдаёт их отдельным блоком В ДОПОЛНЕНИЕ к
-- узким. false у всех существующих строк: старые проекты, их базы и запуски не
-- меняются. Добавление столбца с константой по умолчанию не переписывает таблицу.
alter table public.ve_hypotheses add column if not exists broad boolean not null default false;

comment on column public.ve_hypotheses.broad is
  'VE2: широкая гипотеза уровня сектора для ежедневного добора (true) или обычная узкая (false).';
