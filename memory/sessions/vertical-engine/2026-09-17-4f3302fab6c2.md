---
{
  "id": "2026-09-17-4f3302fab6c2",
  "title": "Adaptive checked batches, candidate priority, and shared company facts",
  "domain": "vertical-engine",
  "kind": "session",
  "status": "verified",
  "scope": "codex/ve2-preview-recovery-scale tracking Sergey; implementation after f37fa25da, not released",
  "actor": "codex/e0e8",
  "sources": [
    "docs/design/2026-09-17-ve2-adaptive-collection.md; adaptiveCollection.ts; companyFacts.ts; baseCollectConstruct.test.ts; llmRetry.test.ts; PGlite migration verification"
  ],
  "recorded_at": "2026-09-17",
  "created_at": "2026-09-17T18:06:14.519601+00:00",
  "checked_at": "2026-09-17",
  "review_after": "2026-10-17"
}
---

Implemented all three requested VE2 cost optimizations in worktree e0e8, based on merge f37fa25da of deployed main 51515d60f into Sergey-linked working branch. No release/production mutations were performed.

- Adaptive collection: up to 100 input companies per fully checked batch, durable ready-email baseline (private hashed state), per-base journal spend window including retries, two poor batches (<5% ready yield or known AI+Serper cost >$0.05/contact) select an untried/better source. At most two saved replan attempts / six total source strategies. Original restrictions and hypothesis relevance checks remain. New source plan invalidates incompatible population estimate. Empty first source does not stop a still-pending alternative. Preserve source tails and reserved child IDs across requeues/restarts; a partial/error batch never becomes a poor-result observation.
- Candidate priority: fresh verified public-site facts/profile first; then usable sites, descriptions and existing email. Stable ties; low-information candidates retained. Shared verified website fills blank source URL before optional paid discovery.
- Public company fact store: new ve_company_fact_pages migration, 7-day observation TTL, INN or name+full address key, identity verification before sharing, neutral full page text/links so different hypotheses refocus separately. No shared relevance/SMTP verdicts. Concurrent page reads coalesce; independent cancellation; stale SQL writer cannot overwrite newer observation. Cache absence/failure falls back to existing live checks. Search cache owned by other developer (30-day policy) remains unchanged.
- UI shows latest completed batch's candidates/new ready contacts/estimated AI+Serper subtotal and whether accounting is incomplete. Never exposes pending recipient hashes.

Verification: complete existing suite 225 files / 2473 tests passed (~61s), strict TS + VE worker bundle passed, lint has zero errors and one pre-existing setState-in-effect warning in Step4Base. Extended existing test blocks only (no new it/files): source switch, bounded replan preserving restrictions, empty-source fallback, source-tail reuse, fact freshness/ownership, different-hypothesis evidence, cancellation and client-state stripping. Migration executed twice in isolated PGlite: grants/access boundaries and stale-write guard passed. Final targeted regression run additionally covers cancellation before HTTP starts.

Release boundary: user revoked autonomous deployments. Push Sergey only. Fact store needs normal migration + app/VE worker release; actual new savings / quality changes require matched production measurement afterward, no threefold-savings guarantee.

User screenshot PR #2563 drain-worker.sh conflict: both branches independently fixed unsafe dotenv sourcing. Local merge kept existing non-evaluating Python parser (BOM/CRLF/quotes). Published exact f37fa25dafc076a8c0f21d697b7831ab93bebd51 to Sergey; GitHub confirmed PR mergeable=true. Local git HTTPS/SSH transport failed (TLS / synthetic DNS), while gh API worked. Git Data API published existing tree/commit with exact SHA, then fast-forward-only reference update; did not merge PR, touch test/main, or deploy. Recheck remote before future writes; never force over another developer's work.
