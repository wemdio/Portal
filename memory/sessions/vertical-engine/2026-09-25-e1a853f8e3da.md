---
{
  "id": "2026-09-25-e1a853f8e3da",
  "title": "Profit Gateway: use saved business facts when website extraction times out",
  "domain": "vertical-engine",
  "kind": "session",
  "status": "verified",
  "scope": "codex/ve2-preview-recovery-scale, parent d50a24a49; siteProfile code and offline regression; release pending",
  "actor": "codex/ve2-preview-recovery-scale",
  "sources": [
    "/Users/cybermart/.codex/handoffs/ve2-profit-gateway-20260925/findings.md; app/src/lib/verticalEngineV2/stages/siteProfile.ts; app/tests/lib/vertical-engine-v2/llmRetry.test.ts"
  ],
  "recorded_at": "2026-09-25",
  "created_at": "2026-09-25T12:15:48.525190+00:00",
  "checked_at": "2026-09-25",
  "review_after": "2026-10-25"
}
---

# Profit Gateway research timeout

2026-09-25 read-only production SQL confirmed three Profit Gateway projects failed at site_profile with `VE operation timeout after 60000ms: website extraction`. Latest project 93b771e9-bcde-416a-9145-b39cdfad229a (created 2026-09-24, /ru URL) has two failed research jobs with five attempts each, latest fcf92b33-53b2-4cc8-8019-12063b62fc77 finished 2026-09-24 17:50 UTC. Its saved business_override is 1660 characters and describes the actual service; no client_brief or site_profile existed. Older /rus and root/en projects also failed similarly; the project named en is market=ru in production. No production changes made.

Current direct HTTP GET from both Mac and production host returned 200 in less than one second, 1440-byte HTML with a Svelte JavaScript bootstrap and no business text. This establishes a JavaScript shell, not a down website. It does not measure the parser's proxy route latency. fetchAndExtract uses HTML extraction and alternate/about URLs; the VE2 wrapper enforces a 60-second deadline. runSiteProfileStage awaited this read unconditionally before reading clientBrief. business_override was previously used only later in hypotheses, so existing manual facts could not rescue the first stage.

Implementation scoped to VE2 siteProfile stage and RU/EN profile prompts: use supplied manual business description as an explicit source; on typed website-extraction deadline with manual description or nonempty compiled client brief, continue using those facts, mark site_thin and site_fetch_error=timeout, skip extra crawling and site case extraction, preserve old cases. No source -> existing error/retry; DNS/access and arbitrary failures do not trigger fallback; parent cancellation propagates before model/write. Successful site reads retain normal processing and clear the timeout marker. Shared website parser and legacy/ENG backend unchanged.

User explicitly approved one additional regression in existing llmRetry.test.ts; no new test file. Mocked offline checks cover real IO deadline via fake time, RU/EN, manual description and uploaded brief, empty input, DNS/access failure, cancellation, late result, healthy path, preserved cases. Full suite 289 suites / 3194 passed, 2 skipped, 57.513 seconds. ESLint exit 0 with 254 existing repository warnings. Full npm run typecheck:strict completed with exit 0 (route generation and all five TypeScript partitions).

Release boundary: commit/push to Sergey only, per user's established branch instruction. No deploy, production retry, paid model probe or message to Gleb performed. After deployment, retry research in the latest existing /ru project, which already contains a manual description; recreating the project is unnecessary. Older projects without manual facts are not rescued by this fallback until such facts are supplied.
