---
{
  "id": "2026-09-16-be2841c488ad",
  "title": "Parallel bases and bounded extra constructor capacity",
  "domain": "vertical-engine",
  "kind": "session",
  "status": "verified",
  "scope": "codex/ve2-preview-recovery-scale from e24317089; local tests; production GET 2026-09-15 23:56 UTC",
  "actor": "codex/01a07c5e-5441-7350-b7c9-a54d82d40ee1",
  "sources": [
    "verticalEngineV2/jobQueue.ts; tools/baseConstructorCapacity.ts; worker/baseConstructor.ts; docker-compose.prod.yml; Jest213/2405"
  ],
  "recorded_at": "2026-09-16",
  "created_at": "2026-09-16T00:10:54.669918+00:00",
  "checked_at": "2026-09-16",
  "review_after": "2026-10-16"
}
---

## Request and verified baseline
User requests more parallel work so current bases can finish by morning. Local branch codex/ve2-preview-recovery-scale, parent e24317089. Push to Sergey was previously authorized; release remains pending.

Read-only production at 2026-09-15 23:56 UTC: constructor 12 processing / 24 pending. All but the manual MEKOMO job (25,268 rows) were preview or validation batches with <=200 input rows. Masha's 32-email child still pending. Three SMTP /health endpoints returned200; this is not an SMTP load test. Host has64GB RAM, ~47GB available; sampled BC RSS173-184MB, VE673MB/8GB.

## Implementation
VE queue previously serialized an entire project, so five projects could not use eight slots fully. New production default16 jobs, at most4 independent bases per project. base_collect/base_analyze/template share a per-base exclusion; research/other operations remain project-exclusive. Waiting exclusive operations form a FIFO barrier. One queue-owner process remains mandatory. Project usage read/modify/write is serialized separately with10s IO deadlines; aggregate failure logs a warning without replaying paid work. Provider usage journal remains authoritative.

BC retains12 general slots and adds up to24 small slots (BASE_CONSTRUCTOR_PREVIEW_SLOTS=2 per replica). Extras accept only1..200 input rows, validation-only or known interactive_preview steps. General capacity is tracked separately to avoid starving bulk jobs. RSS>=65% cgroup limit prevents another concurrent start; missing cgroup limit disables extras. Existing CAS/run-token ownership remains. Stale saved jobs resume before new pending jobs; extra claims do not affect the general FIFO alternation. Per-process SMTP ceiling20 shares capacity across jobs; per-job limit10 remains, BC total ceiling240. No email or relevance gate is weakened.

## Validation and limits
213 suites / 2405 tests PASS55.413s on Node22; no new test files or it() cases. Extended scenarios cover375 jobs with16 concurrent slots, same-project independent bases, same-base exclusion, exclusive barrier, concurrent usage increments, small-job eligibility, memory admission, shared SMTP limits/failure release, stale/fresh selection. tsc and ESLint PASS; worker bundles build. YAML merge parsing confirms all12 replicas inherit preview slots2 and VE default16. The final bulk-slot/high-RSS guard was rechecked in its targeted test. Runtime throughput and morning completion are not yet verified.

## Release and continuation
No deployment or production data mutation performed. One release can include this change and e24317089. Verify actual env, active job counts, memory, SMTP outcomes, ready growth, and05:00MSK handoff after release. Previously stopped technical preparations still need specifically authorized normal continuation; empty analyzed bases are not repaired by scaling. Existing read-only heartbeat automation id automation watches the five projects until09:00MSK. Local handoff: /Users/cybermart/.codex/handoffs/ve2-masha-live-20260916/README.md.
