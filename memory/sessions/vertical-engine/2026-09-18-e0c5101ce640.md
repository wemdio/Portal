---
{
  "id": "2026-09-18-e0c5101ce640",
  "title": "Separate cheap model for VE2 source planning and base analysis",
  "domain": "vertical-engine",
  "kind": "session",
  "status": "verified",
  "scope": "codex/ve2-preview-recovery-scale based on 42078dbba; current task pushed to Sergey; no deployment",
  "actor": "codex/ve2-preview-recovery-scale",
  "sources": [
    "app/src/lib/verticalEngineV2/llm.ts; stages/baseCollect.ts; stages/baseAnalyze.ts; docker-compose.prod.yml; full Jest and TypeScript checks"
  ],
  "recorded_at": "2026-09-18",
  "created_at": "2026-09-18T00:48:16.307292+00:00",
  "checked_at": "2026-09-18",
  "review_after": "2026-10-18"
}
---

## Request and result

User approved removing the remaining expensive GPT-5.5 calls from source planning and final base composition analysis after a read-only production audit. Added the separate `collection` role, `VE_MODEL_COLLECTION`, defaulting to `deepinfra/deepseek-v4-flash-0731`. Routed all three source-plan/repair calls in `stages/baseCollect.ts` and the call in `stages/baseAnalyze.ts` through it. Compose exposes the override with the same default.

Research, other bulk callers, letters, admission confirmation and SMTP validation retain their existing routing/contracts. In particular GPT-5.5 may still appear for research, hypotheses and their other supporting stages. No saved plans/checkpoints are invalidated or regenerated. No automatic expensive fallback was introduced. Production deployment was not performed; the VE2 worker image and configuration must be updated before the new routing takes effect. Actual production cost and quality after this extension remain unmeasured.

## Verification

Extended existing assertions only (no new test cases/files): transient retry sends both HTTP requests to the cheap collection model despite the expensive bulk override; collection override/blank fallback are independent of research/chain; adaptive replanning uses the collection role and resumes without a second paid plan.

Full Jest suite: 225 suites / 2473 tests passed in 60.388 seconds. TypeScript noEmit, targeted ESLint, VE2 worker esbuild bundle and git diff checks passed. Logs: `/tmp/ve2-collection-model-tests.log`, `/tmp/ve2-collection-model-typecheck.log` on this Mac. These checks verify routing/lifecycle, not model-output quality equivalence on a representative dataset.

Prior diagnostic evidence: shared memory `sessions/vertical-engine/2026-09-18-e04530dd82f3.md`; local private audit at `~/.codex/handoffs/ve2-gpt55-cost-audit-20260918/usage.jsonl`.
