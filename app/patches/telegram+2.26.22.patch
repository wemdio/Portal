diff --git a/node_modules/telegram/extensions/BinaryReader.js b/node_modules/telegram/extensions/BinaryReader.js
index 26a439a..32c6121 100644
--- a/node_modules/telegram/extensions/BinaryReader.js
+++ b/node_modules/telegram/extensions/BinaryReader.js
@@ -170,6 +170,53 @@ class BinaryReader {
              * chance of it being a manually parsed value like bool!
              */
             const value = constructorId;
+            if (value === 0xfc89f7f3) {
+                // dialog#fc89f7f3 — Telegram's newer wire format for Dialog. It
+                // adds unread_poll_votes_count:int (4 bare bytes) between
+                // unread_reactions_count and notify_settings; every other field
+                // matches the older dialog#d58a08c6 that gramJS 2.26.22 knows.
+                // Without this shim every dialog in messages.GetDialogs crashes
+                // the whole response and breaks accounts with thousands of
+                // dialogs. We read the new layout manually and return an
+                // instance of the existing Dialog class so every downstream
+                // check (instanceof Api.Dialog, .peer/.unreadCount/…) just works.
+                const Dialog = AllTLObjects_1.tlobjects[0xd58a08c6];
+                if (Dialog) {
+                    const flags = this.readInt();
+                    const peer = this.tgReadObject();
+                    const topMessage = this.readInt();
+                    const readInboxMaxId = this.readInt();
+                    const readOutboxMaxId = this.readInt();
+                    const unreadCount = this.readInt();
+                    const unreadMentionsCount = this.readInt();
+                    const unreadReactionsCount = this.readInt();
+                    const unreadPollVotesCount = this.readInt(); // new bare int field
+                    const notifySettings = this.tgReadObject();
+                    const pts = (flags & 1) ? this.readInt() : undefined;
+                    const draft = (flags & 2) ? this.tgReadObject() : undefined;
+                    const folderId = (flags & 16) ? this.readInt() : undefined;
+                    const ttlPeriod = (flags & 32) ? this.readInt() : undefined;
+                    return new Dialog({
+                        flags,
+                        pinned: !!(flags & 4),
+                        unreadMark: !!(flags & 8),
+                        viewForumAsMessages: !!(flags & 64),
+                        peer,
+                        topMessage,
+                        readInboxMaxId,
+                        readOutboxMaxId,
+                        unreadCount,
+                        unreadMentionsCount,
+                        unreadReactionsCount,
+                        notifySettings,
+                        pts,
+                        draft,
+                        folderId,
+                        ttlPeriod,
+                        unreadPollVotesCount,
+                    });
+                }
+            }
             if (value === 0x997275b5) {
                 // boolTrue
                 return true;
@@ -183,18 +230,26 @@ class BinaryReader {
                 const temp = [];
                 const length = this.readInt();
                 for (let i = 0; i < length; i++) {
-                    temp.push(this.tgReadObject());
+                    try {
+                        const obj = this.tgReadObject();
+                        if (obj !== undefined) temp.push(obj);
+                    } catch (_e) {
+                        break;
+                    }
                 }
                 return temp;
             }
             clazz = core_1.coreObjects.get(constructorId);
             if (clazz === undefined) {
-                // If there was still no luck, give up
-                this.seek(-4); // Go back
-                const pos = this.tellPosition();
-                const error = new errors_1.TypeNotFoundError(constructorId, this.read());
-                this.setPosition(pos);
-                throw error;
+                // Unknown constructor — skip remaining bytes instead of crashing
+                if (!BinaryReader._unknownWarnedIds) BinaryReader._unknownWarnedIds = new Set();
+                if (!BinaryReader._unknownWarnedIds.has(constructorId)) {
+                    BinaryReader._unknownWarnedIds.add(constructorId);
+                    console.warn(`[gramjs-patch] Unknown TL constructor ${constructorId} (0x${constructorId.toString(16)}), skipping. Update 'telegram' package when a new version is available.`);
+                }
+                this.seek(-4);
+                this.read();
+                return undefined;
             }
         }
         return clazz.fromReader(this);
@@ -210,7 +265,12 @@ class BinaryReader {
         const count = this.readInt();
         const temp = [];
         for (let i = 0; i < count; i++) {
-            temp.push(this.tgReadObject());
+            try {
+                const obj = this.tgReadObject();
+                if (obj !== undefined) temp.push(obj);
+            } catch (_e) {
+                break;
+            }
         }
         return temp;
     }
diff --git a/node_modules/telegram/tl/api.js b/node_modules/telegram/tl/api.js
index c8b6564..0ca1842 100644
--- a/node_modules/telegram/tl/api.js
+++ b/node_modules/telegram/tl/api.js
@@ -150,7 +150,22 @@ function getArgFromReader(reader, arg) {
         const len = reader.readInt();
         arg.isVector = false;
         for (let i = 0; i < len; i++) {
-            temp.push(getArgFromReader(reader, arg));
+            // [gramjs-patch] Telegram периодически добавляет поля в существующие
+            // TL-конструкторы внутри Vector (User, Channel, Message и т.п.).
+            // gramJS 2.26.22 читает их по старой схеме и падает RangeError
+            // "value of 'offset' is out of range" на чтении лишнего int32. При
+            // таком сигнале прерываем Vector — partial-результат лучше, чем
+            // потеря всего GetDialogs ответа. Другие ошибки не маскируем.
+            try {
+                temp.push(getArgFromReader(reader, arg));
+            }
+            catch (e) {
+                // ERR_OUT_OF_RANGE — Buffer.readInt32LE дочитал за конец буфера
+                // (реальный сигнал расхождения TL-схемы у extension-объекта).
+                // 'outside buffer bounds' — внутренний gramJS range-check.
+                if (e && (e.code === 'ERR_OUT_OF_RANGE' || (typeof e.message === 'string' && e.message.includes('outside buffer bounds')))) break;
+                throw e;
+            }
         }
         arg.isVector = true;
         return temp;
@@ -261,29 +276,58 @@ function createClasses(classesType, params) {
             init(args) { }
             static fromReader(reader) {
                 const args = {};
+                // [gramjs-patch] Когда у Telegram расходится TL-схема известного
+                // конструктора (Telegram добавил поле, gramJS не знает),
+                // getArgFromReader → reader.readInt() падает RangeError. Без
+                // защиты весь GetDialogs-ответ теряется и аккаунт пропускает
+                // круг, а после 5 подряд получает 6h cooldown. Ловим
+                // RangeError и возвращаем partial-объект с null в недочитанных
+                // полях — лучше частичные данные, чем потеря всего ответа.
+                let _bailedOn = null;
                 for (const argName in argsConfig) {
                     if (argsConfig.hasOwnProperty(argName)) {
                         const arg = argsConfig[argName];
-                        if (arg.isFlag) {
-                            if (arg.type === "true") {
-                                args[argName] = Boolean(args[arg.flagName] & (1 << arg.flagIndex));
-                                continue;
-                            }
-                            if (args[arg.flagName] & (1 << arg.flagIndex)) {
-                                args[argName] = getArgFromReader(reader, arg);
+                        try {
+                            if (arg.isFlag) {
+                                if (arg.type === "true") {
+                                    args[argName] = Boolean(args[arg.flagName] & (1 << arg.flagIndex));
+                                    continue;
+                                }
+                                if (args[arg.flagName] & (1 << arg.flagIndex)) {
+                                    args[argName] = getArgFromReader(reader, arg);
+                                }
+                                else {
+                                    args[argName] = null;
+                                }
                             }
                             else {
-                                args[argName] = null;
+                                if (arg.flagIndicator) {
+                                    arg.name = argName;
+                                }
+                                args[argName] = getArgFromReader(reader, arg);
                             }
                         }
-                        else {
-                            if (arg.flagIndicator) {
-                                arg.name = argName;
+                        catch (e) {
+                            // ERR_OUT_OF_RANGE — Buffer.readInt32LE дочитал за
+                            // конец буфера (TL-схема Telegram расширилась).
+                            // 'outside buffer bounds' — внутренний gramJS check.
+                            const oob = e && (e.code === 'ERR_OUT_OF_RANGE' || (typeof e.message === 'string' && e.message.includes('outside buffer bounds')));
+                            if (oob) {
+                                _bailedOn = argName;
+                                args[argName] = null;
+                                break;
                             }
-                            args[argName] = getArgFromReader(reader, arg);
+                            throw e;
                         }
                     }
                 }
+                if (_bailedOn) {
+                    if (!createClasses._partialWarnedClasses) createClasses._partialWarnedClasses = new Set();
+                    if (!createClasses._partialWarnedClasses.has(fullName)) {
+                        createClasses._partialWarnedClasses.add(fullName);
+                        console.warn('[gramjs-patch] partial parse for ' + fullName + ' at "' + _bailedOn + '" — Telegram TL schema extended; update telegram pkg when a newer release appears.');
+                    }
+                }
                 return new this(args);
             }
             validate() {
