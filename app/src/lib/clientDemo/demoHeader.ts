/**
 * Имя заголовка, которым вкладка помечает себя «демо-вкладкой».
 * Вынесено в нейтральный модуль: используется и фронтом (clientFetcher),
 * и сервером (clientApiHelper) — без 'use client' директивы.
 */
export const TAB_DEMO_HEADER = 'x-client-demo';
