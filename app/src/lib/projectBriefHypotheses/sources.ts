/**
 * Знания агентства о реальных источниках сбора B2B-баз. Используется как контекст
 * для AI при генерации гипотез по сбору базы из брифа клиента.
 *
 * Источники описаны на одном языке (русский), потому что и бриф клиента, и сам
 * вывод гипотез — на русском.
 */

import type { ExportBaseCatalogItem, ExportBaseCategory } from './exportBaseCatalog';

/**
 * Названия источников, которые ОБЯЗАТЕЛЬНО должны фигурировать в промпте.
 * Используется тестом `projectBriefSourcesPrompt.test.ts` чтобы при правках
 * `LEAD_SOURCES_KNOWLEDGE` мы не выкинули кого-то по неосторожности.
 */
export const REQUIRED_SOURCE_NAMES = [
  'HH',
  'Руспрофайл',
  'Селеком',
  'Сбис',
  'Поисковая выдача',
  'Яндекс.Карты',
  'Google Maps',
  'Crypto Payments',
  'Сигналы',
  'export-base.ru',
] as const;

export const LEAD_SOURCES_KNOWLEDGE = `# Источники сбора B2B-баз, доступные нашему агентству

## 1. HH (HeadHunter, hh.ru)
Парсим публичные вакансии работодателей. Что можно вытащить:
- Список компаний по фильтру: ключевое слово в названии вакансии, регион, отрасль, размер компании.
- Сигналы найма (рост команды, открытие новых направлений, нехватка определённой роли).
Когда применять: когда у клиента ЦА — компании, которые активно нанимают (значит, есть бюджет, рост, потребность в смежных услугах). Хорошо комбинируется с обогащением контактами через сайт.
Пример фильтра: "Крупные SaaS-компании из Москвы и Санкт-Петербурга, которые нанимают B2B-маркетологов или sales-менеджеров за последние 30 дней."

## 2. Реестровые базы: Руспрофайл, Селеком, Сбис
Парсим открытые реестры юрлиц с глубокой фильтрацией:
- ОКВЭД (основной + дополнительные) — точная привязка к виду деятельности.
- Выручка по бухгалтерской отчётности (диапазоны).
- ССЧ (среднесписочная численность сотрудников) — прокси на размер бизнеса.
- Регион регистрации, форма собственности (ООО/ИП/АО), статус (действующая/ликвидирована).
- Дата регистрации (молодые / зрелые), наличие лицензий, госконтракты.
Когда применять: классический B2B по чётко описанной ICP с понятными отраслевыми и финансовыми критериями.
Пример фильтра: "ООО с ОКВЭД 47.78 (рознич. торговля), выручка 50-500 млн ₽, ССЧ 10-100, ЦФО."

## 3. Поисковая выдача (Google + Яндекс)
Используем язык поисковых запросов (site:, intitle:, inurl:, "контакты", "официальный сайт", "директор по закупкам" и т.п.) для парсинга релевантных компаний и страниц с контактами.
Когда применять: когда нужны небольшие тематические базы, нишевые игроки, у которых нет заметного кода ОКВЭД, а есть конкретный продукт/услуга на сайте.
Пример: запросы вида '"производитель тротуарной плитки" Москва "контакты" -site:hh.ru', '"внедрение Bitrix24" "официальный сайт"'.

## 4. Яндекс.Карты и Google Maps
Парсим карточки организаций с локациями, сайтами, телефонами, рубриками и отзывами.
Когда применять: локальный B2B (HoReCa, ритейл, услуги в городах), офлайн-точки, медицина/красота/автосервисы.
Пример фильтра: "Все автосервисы Самары и Тольятти с рейтингом ниже 4.0 и количеством отзывов >20" — отдельная боль = повод писать про SERM/репутацию.

## 5. Crypto Payments (наш парсер /parsers/crypto-payments)
По домену проверяет, использует ли сайт крипто-эквайринг (NowPayments, CoinPayments, BTCPay, Cryptomus и др.) и какой именно.
Когда применять: клиент продаёт крипто-эквайринг, on/off-ramp, AML/KYC сервисы, инфраструктуру для крипто-бизнеса. Запускается поверх любой уже собранной базы доменов.

## 6. Сигналы с сайтов (кнопка «Сигналы» в /tools/databases)
Сканер сайтов из таблицы баз: возвращает рекламные пиксели (Яндекс.Директ, VK Pixel, myTarget, Google Ads), CRM (amoCRM, Битрикс24, RetailCRM, HubSpot), чаты (JivoSite, Carrot Quest), call-tracking (Calltouch, CoMagic), онлайн-оплату (ЮKassa, CloudPayments, Tinkoff, Робокасса), email-маркетинг (Unisender, Mindbox), CMS, ссылки на маркетплейсы. Это даёт «стек» каждой компании и профили-комбинации (например, «тратит на рекламу, но нет CRM»).
Когда применять: когда оффер клиента завязан на конкретной технологии или дыре в стеке (CRM-внедрение, аналитика, перевод с одного эквайринга на другой, переезд с маркетплейсов на D2C). Запускается поверх базы доменов.

## 7. export-base.ru — каталог готовых баз
Платный каталог уже собранных баз по тематикам. Часто можно сэкономить дни ручного сбора, купив готовую базу. Категории: ОКВЭД, отраслевые рубрики, типы компаний (с ЛПР, с автопарком, с лицензиями, по системе налогообложения), тематические базы сайтов (CMS, по нишам), маркетплейс-продавцы (WB/OZON/ЯМ), участники профильных выставок, должники по налогам, госзакупки 44/223-ФЗ.
Когда применять: когда тематика клиента совпадает с одной из готовых категорий — это быстрее и дешевле, чем парсить с нуля. Полный список категорий передан моделью отдельным блоком ниже.
`;

const RUSSIAN_STOPWORDS = new Set([
  'и',
  'в',
  'на',
  'с',
  'по',
  'для',
  'не',
  'к',
  'от',
  'до',
  'из',
  'у',
  'или',
  'мы',
  'наш',
  'наша',
  'свой',
  'который',
  'это',
  'эти',
  'тот',
  'все',
  'весь',
  'без',
  'за',
  'при',
  'про',
  'но',
  'как',
  'так',
  'же',
  'бы',
  'клиент',
  'компания',
  'продукт',
  'услуга',
  'услуги',
  'продаём',
  'продаем',
  'делаем',
]);

function tokenizeRu(text: string): string[] {
  return (text.toLowerCase().match(/[\p{L}\p{N}]+/gu) ?? []).filter(
    (t) => t.length >= 4 && !RUSSIAN_STOPWORDS.has(t),
  );
}

const TOP_PRIORITY: ExportBaseCategory[] = ['ТОП', 'Рубрика', 'Тип компании'];

/**
 * Сужает каталог до топ-N записей, наиболее релевантных тексту брифа,
 * по простому keyword-overlap (количество общих токенов >=4 символов).
 * Если бриф пуст или нет совпадений — возвращает первые N "TOП"-категорий.
 */
export function selectRelevantCatalog(
  briefText: string,
  catalog: ExportBaseCatalogItem[],
  limit: number,
): ExportBaseCatalogItem[] {
  const safeLimit = Math.max(1, Math.floor(limit));
  const briefTokens = new Set(tokenizeRu(briefText));

  if (briefTokens.size === 0) {
    return catalog.filter((i) => TOP_PRIORITY.includes(i.category)).slice(0, safeLimit);
  }

  const scored = catalog
    .map((item) => {
      const tokens = tokenizeRu(item.name);
      let score = 0;
      for (const t of tokens) if (briefTokens.has(t)) score += 1;
      // лёгкий буст для топ-категорий
      if (TOP_PRIORITY.includes(item.category)) score += 0.25;
      return { item, score };
    })
    .filter((entry) => entry.score > 0)
    .sort((a, b) => b.score - a.score);

  if (scored.length === 0) {
    return catalog.filter((i) => TOP_PRIORITY.includes(i.category)).slice(0, safeLimit);
  }

  // защита от дублей по URL
  const seen = new Set<string>();
  const result: ExportBaseCatalogItem[] = [];
  for (const { item } of scored) {
    if (seen.has(item.url)) continue;
    seen.add(item.url);
    result.push(item);
    if (result.length >= safeLimit) break;
  }
  return result;
}

const MAX_BRIEF_CHARS = 8000;

export interface BuildHypothesesPromptInput {
  briefText: string;
  catalog: ExportBaseCatalogItem[];
}

export interface HypothesesPrompt {
  system: string;
  user: string;
}

/** Формирует system + user prompt для модели. */
export function buildHypothesesPrompt({ briefText, catalog }: BuildHypothesesPromptInput): HypothesesPrompt {
  const safeBrief = (briefText ?? '').slice(0, MAX_BRIEF_CHARS);

  const catalogBlock = catalog
    .map((item) => `- [${item.category}] ${item.name} — ${item.url}`)
    .join('\n');

  const system = `Ты — старший лид-ресечер B2B-агентства Polza. На основе клиентского брифа подбираешь 5–10 гипотез по сбору базы потенциальных клиентов. Используй ТОЛЬКО источники из перечня ниже — не выдумывай чужие сервисы, не предлагай LinkedIn Sales Navigator или Apollo, если их нет в списке.

Каждая гипотеза должна быть конкретной и пригодной для запуска нашим специалистом, а не общей "найдите всех в IT".

${LEAD_SOURCES_KNOWLEDGE}

ФОРМАТ ОТВЕТА — markdown без преамбулы и без заключения. Структура каждой гипотезы:
### Гипотеза N: <короткое название>
- Источник: <название одного из 7 источников выше или конкретная страница из export-base>
- Конкретные фильтры/запросы: <что именно ввести/настроить>
- Почему подходит брифу: <1–2 предложения связи с ЦА из брифа>
- Ожидаемый объём: <грубая оценка количества компаний>
- Риски/нюансы: <опционально, если есть>

Не пиши общих рассуждений вне гипотез. Не дублируй гипотезы. Если бриф крайне неполный — укажи это в первой гипотезе и предложи уточняющие вопросы заказчику.`;

  const user = `КЛИЕНТСКИЙ БРИФ:
"""
${safeBrief}
"""

РЕЛЕВАНТНЫЕ ГОТОВЫЕ БАЗЫ ИЗ КАТАЛОГА export-base.ru (можно ссылаться напрямую):
${catalogBlock}

Сгенерируй 5–10 гипотез по сбору базы по этому брифу. Используй только источники из системного промпта. Учитывай готовые категории каталога export-base.ru — они часто экономят время по сравнению с парсингом.`;

  return { system, user };
}
