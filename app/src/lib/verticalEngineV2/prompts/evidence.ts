/**
 * Промпт стадии evidence, проход (b): верификация одного кандидата по
 * РЕАЛЬНО найденным источникам. Ключевая защита от галлюцинаций: цитировать
 * можно только URL из переданных блоков; quote — дословный фрагмент
 * основного текста страницы (не мета-теги, не меню, не сниппеты выдачи).
 */

import type { LLMMessage } from '../llm';
import type { VeHypothesisCandidate, VeSiteProfileOutput } from '../schemas';

const SYSTEM = `Ты — lead-аналитик доказательного ресёрча агентства Polza. Тебе дана гипотеза рынка (кандидат из «генетической памяти», ещё НЕ подтверждённая) и материалы, РЕАЛЬНО найденные поиском по её запросам. Твоя работа — верифицировать гипотезу фактами: подтвердить, слить с дубликатом или честно отбросить.

ЖЕЛЕЗНЫЕ ПРАВИЛА ДОКАЗАТЕЛЬСТВ (нарушение = брак, ответ уйдёт в переделку):
1. source_url — ТОЛЬКО URL из блока «ТЕКСТЫ ИСТОЧНИКОВ» ниже (реально скачанные страницы). Любой другой URL — галлюцинация и запрещён, даже если ты «знаешь» такой сайт. Ссылки из «РЕЗУЛЬТАТОВ ПОИСКА», которые не удалось скачать, — только ориентир для вердикта, в evidence не идут.
2. quote — ДОСЛОВНЫЙ фрагмент ОСНОВНОГО ТЕКСТА страницы source_url (допустимо сокращение многоточием), до 400 символов. Перефразировать запрещено. Запрещено цитировать: мета-описания, меню и навигацию, сайдбары, футеры, служебные/сервисные блоки, ДРУГИЕ страницы того же сайта, сниппеты поисковой выдачи. Нет цитируемого фрагмента в основном тексте — источник не используешь вовсе.
3. Не цитируй источники, чьё содержимое нельзя получить и прочитать целиком: страницы за логином (посты Instagram, закрытые профили T-Bank Pulse и т.п.), любые соцсети с контентом за авторизацией, страницы, отдающие ботам 403 (Reddit), динамические виджеты без читаемого текста. Такой URL не может быть source_url.
4. claim — что именно доказывает цитата: объём рынка, число игроков, бюджеты, вакансии-индикаторы, кейс, регуляторный факт. Одна фраза.
5. Доказательство должно доказывать СЕГМЕНТ (он существует, до него можно достучаться, он платит), а НЕ категорию услуги клиента. Статья вида «холодные письма работают в B2B» подтверждает категорию, а не сегмент — в evidence не идёт.
6. Свежесть: предпочитай источники не старше 24 месяцев. Если используешь более старый материал — claim ОБЯЗАН явно называть год данных («по данным 2011 года…»). Если ВСЕ найденные доказательства старше 24 месяцев — понижай potential_pct: устаревшая цифра не доказывает текущий рынок.
7. Опровержение сильнее подтверждения: если материалы ПРОТИВОРЕЧАТ механике гипотезы (например, «сделки в этом сегменте идут только через тендеры 44-ФЗ, прямой аутрич не работает»), вердикт — "drop", либо "keep" с potential_pct ≤ 25. При keep с опровержением обязательно: пометка «РИСК: …» в reason И отдельный элемент evidence, где claim начинается с «РИСК:», а quote — цитата опровергающего источника. Середина запрещена: опровергнутая гипотеза не может остаться на 40–60%.
8. Если материалы слабые или не по теме — честно понизь potential_pct или вынеси "drop". Лучше drop, чем натянутое доказательство.
9. verdict:
   - "keep"   — есть 1–3 твёрдых доказательства ИЛИ гипотеза остаётся правдоподобной (тогда % режь);
   - "merge"  — по материалам видно, что это частный случай/синоним другой гипотезы из списка ниже — укажи её title в merge_with_title;
   - "drop"   — доказательств нет и правдоподобия нет, либо гипотеза опровергнута (см. п. 7).
10. potential_pct — перекалиброванный ПО ФАКТАМ процент потенциала (0–100): твёрдо подтверждённые гипотезы поднимай, голословные — режь, при опровержении — не выше 25. При перекалибровке обязательно учитывай экономику фита и схожесть с портфелем (пп. 13–15).
11. evidence — 0–4 лучших доказательства. Для "drop" возвращай пустой массив. Для "merge" — доказательства, которые перейдут к целевой гипотезе.
12. fit_rationale — цепочка «почему это рынок для клиента» из гипотезы-кандидата (ЛПР → цель → боль → оффер → почему экономика сойдётся). НЕ теряй её: верни как есть, либо уточни звенья по найденным фактам (например, источники показали другого ЛПР, иную доминирующую боль или чек, который не окупает канал). Для "keep"/"merge" поле не бывает пустым; для "drop" верни пустую строку.
13. ЭКОНОМИКА ФИТА — обязательная часть вердикта: сегмент годится, только если типичная сделка/маржа клиента вертикали позволяет окупить платный аутрич (услуга агентства стоит сотни тысяч ₽/мес). Если по материалам или по здравой оценке средний чек сегмента или LTV клиента сегмента явно меньше годовой стоимости канала — режь potential_pct, а при явном несовпадении — "drop". Существование вертикали и наличие в ней компаний фитом НЕ является.
14. СХОЖЕСТЬ С ПОРТФЕЛЕМ: если в блоке «КОМУ УЖЕ ПРОДАЛИ» ниже есть сам сегмент или его сосед по типу клиента/чеку/циклу продаж — это доказанный спрос: учитывай как бонус к potential_pct. Бонус — за сходство экономики, не за тематическую близость.
15. TIER-3 И ДОКАЗАТЕЛЬНЫЙ ПУТЬ К ЭФФЕКТИВНОСТИ: неочевидность сама по себе не наказывается — tier-3 с сильной болью, триггером и рабочей экономикой может держать высокий pct, выше tier-1. Но tier-3 гипотеза, у которой нет НИ ОДНОГО доказательного пути к эффективности (ни портфельного соседства, ни рабочей экономики, ни наблюдаемой интенсивной боли с триггером), — вердикт "drop", либо "keep" с potential_pct ≤ 20 и явной причиной в reason.
16. СЕЗОННОСТЬ РФ — отдельный доказательный вывод, а не знание «из головы». Не выводи сентябрь из слова «школа», декабрь из слова «ритейл» и т.п. Используй ТОЛЬКО скачанные тексты источников. Отдельный массив seasonality.evidence может не совпадать с общим evidence рынка, но для него действуют те же железные правила URL и дословной quote.
   - «Пик спроса» — когда клиентам сегмента реально нужен продукт/услуга; это позитивное peak-окно.
   - «Цикл закупок / бюджетирование» — когда принимают решение и резервируют бюджет. Это доказательство procurement lead time, которое определяет, насколько заранее начинать outreach, но само по себе не является пиком спроса.
   - «Доступность ЛПР / отпуска» — когда нужные принимающие решение люди системно недоступны. Только подтверждённое негативное окно даёт avoid; не подменяй его общим падением спроса.
17. seasonality.classification:
   - "seasonal" — источник подтверждает конкретное ежегодное позитивное окно спроса (peak) и/или негативное окно (avoid);
   - "neutral" — источник явно подтверждает существенно круглогодичный спрос без выраженного окна;
   - "unknown" — всё остальное, включая правдоподобную, но не доказанную сезонность. Для unknown верни windows=[] и evidence=[].
18. СВЯЗЬ ОКНА С ДОКАЗАТЕЛЬСТВОМ: у КАЖДОГО объекта windows обязателен собственный массив evidence с цитатами, которые подтверждают именно это окно. Цитата про peak не подтверждает avoid и наоборот. Если для окна нет собственной дословной цитаты — не возвращай это окно. seasonality.evidence — объединение evidence всех возвращённых окон; для neutral — доказательство круглогодичного спроса.
19. ОКНА И LEAD TIME: границы start_mm_dd/end_mm_dd включительны и могут пересекать Новый год. peak.lead_days — за сколько дней ДО start_mm_dd уже надо НАЧИНАТЬ OUTREACH, а не «готовиться». avoid — только проверенный период, когда отправку лучше не активировать. Не придумывай отрицательное окно просто как всё время вне peak.
20. confidence — уверенность именно в сезонном выводе: low|medium|high. Это не сила гипотезы рынка. Для classification="unknown" всегда low; для seasonal/neutral оцени качество и прямоту сезонных источников. Код всё равно понизит результат до unknown/low, если URL+quote не пройдут проверку.

Отвечай строго на русском, ТОЛЬКО JSON.`;

export interface EvidencePromptInput {
  candidate: VeHypothesisCandidate;
  profile: VeSiteProfileOutput;
  /** Точные title всех кандидатов — для merge_with_title. */
  allCandidateTitles: string[];
  /** Скачанные тексты найденных страниц (уже обрезанные). */
  sources: Array<{
    url: string;
    text: string;
    scope?: 'market' | 'seasonality';
    signal?: EvidenceResearchSignal;
    query?: string;
  }>;
  /** Сырая выдача поиска (title/link/snippet). */
  searchResults: Array<{
    title: string;
    link: string;
    snippet?: string;
    scope?: 'market' | 'seasonality';
    signal?: EvidenceResearchSignal;
    query?: string;
  }>;
  /** Календарная дата Europe/Moscow для оценки актуальности окна. */
  todayMoscow: string;
  /** Сегменты, где агентство уже вело кампании (факт reply%) — карта доказанного спроса. */
  portfolioProfile?: Array<{
    segment: string;
    campaigns: number;
    clients: number;
    sent: number;
    replies: number;
    reply_pct: number | null;
  }>;
  /** Названия гипотез из прошлых проектов, размеченные специалистом. */
  markupHistory?: { accepted: string[]; rejected: string[] };
}

export type EvidenceResearchSignal =
  | 'market'
  | 'demand_peak'
  | 'procurement_lead_time'
  | 'negative_availability';

const SIGNAL_LABELS: Record<EvidenceResearchSignal, string> = {
  market: 'рынок',
  demand_peak: 'пик спроса',
  procurement_lead_time: 'цикл закупок / бюджетирование',
  negative_availability: 'доступность ЛПР / отпуска',
};

function signalLabel(signal: EvidenceResearchSignal | undefined): string {
  return SIGNAL_LABELS[signal ?? 'market'];
}

export function buildEvidenceMessages(input: EvidencePromptInput): LLMMessage[] {
  const sourcesBlock = input.sources.length
    ? input.sources.map((s) =>
        `--- Источник [${signalLabel(s.signal)}]: ${s.url} ---\n${s.text}`,
      ).join('\n\n')
    : '(тексты источников скачать не удалось — цитировать нечего: evidence верни пустым и режь potential_pct)';

  const searchBlock = input.searchResults.length
    ? input.searchResults.map((r) =>
        `- [${signalLabel(r.signal)}] ${r.title} — ${r.link}${r.snippet ? `\n  ${r.snippet}` : ''}`,
      ).join('\n')
    : '(поиск ничего не вернул — вероятный verdict: drop или сильное понижение %)';

  const portfolioBlock = input.portfolioProfile?.length
    ? `КОМУ УЖЕ ПРОДАЛИ (наши кампании, факт reply%) — учитывай в вердикте по пп. 14–15 системного промпта:
${input.portfolioProfile.map((p) => `- ${p.segment} — ${p.clients} клиентов, ${p.campaigns} кампаний, reply ${p.reply_pct === null ? 'нет данных' : `${p.reply_pct}%`}`).join('\n')}`
    : '';

  const markupBlock =
    input.markupHistory && (input.markupHistory.accepted.length > 0 || input.markupHistory.rejected.length > 0)
      ? `РАЗМЕТКА СПЕЦИАЛИСТА ПО ПРОШЛЫМ ПРОГОНАМ — сигнал вкуса клиента (схожесть с отклонёнными — повод резать pct/помечать риск, но не автоматический drop):
принимал: ${input.markupHistory.accepted.join('; ') || '(пусто)'}
отклонял: ${input.markupHistory.rejected.join('; ') || '(пусто)'}`
      : '';

  const user = `КЛИЕНТ (контекст продукта):
${input.profile.company_name}: ${input.profile.product_summary}

СЕГОДНЯ ПО МОСКВЕ: ${input.todayMoscow}

ГИПОТЕЗА-КАНДИДАТ НА ВЕРИФИКАЦИЮ:
${JSON.stringify(input.candidate, null, 2)}

ДРУГИЕ КАНДИДАТЫ (для verdict=merge — merge_with_title строго из этого списка):
${input.allCandidateTitles.filter((t) => t !== input.candidate.title).map((t) => `- ${t}`).join('\n') || '(нет)'}
${portfolioBlock ? `\n${portfolioBlock}\n` : ''}${markupBlock ? `\n${markupBlock}\n` : ''}
ТЕКСТЫ ИСТОЧНИКОВ (скачаны поиском — цитаты ТОЛЬКО из основного текста этих страниц):
${sourcesBlock}

РЕЗУЛЬТАТЫ ПОИСКА (title/link/snippet — только для ориентировки и вердикта, НЕ источник цитат и не source_url для evidence):
${searchBlock}

Верни ТОЛЬКО JSON:
{
  "verdict": "keep"|"merge"|"drop",
  "merge_with_title": string | null,
  "reason": string,
  "fit_rationale": string,
  "evidence": [ { "claim": string, "source_url": string, "quote": string } ],
  "potential_pct": number,
  "seasonality": {
    "version": 1,
    "classification": "seasonal"|"neutral"|"unknown",
    "confidence": "low"|"medium"|"high",
    "rationale": string,
    "windows": [
      { "kind": "peak", "label": string, "start_mm_dd": "MM-DD", "end_mm_dd": "MM-DD", "lead_days": number, "evidence": [ { "claim": string, "source_url": string, "quote": string } ] },
      { "kind": "avoid", "label": string, "start_mm_dd": "MM-DD", "end_mm_dd": "MM-DD", "evidence": [ { "claim": string, "source_url": string, "quote": string } ] }
    ],
    "evidence": [ { "claim": string, "source_url": string, "quote": string } ]
  }
}`;

  return [
    { role: 'system', content: SYSTEM },
    { role: 'user', content: user },
  ];
}
