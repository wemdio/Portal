/**
 * Автозаполнение профиля аккаунта: имя, фамилия, свободный ник и описание.
 *
 * Профили заполняли руками, по одному, и на партии в двадцать аккаунтов это
 * час работы с одинаковым результатом: у половины аккаунтов профиль так и
 * оставался пустым («профиль не прочитан» в списке), а пустой профиль — первое,
 * на что смотрит получатель холодного письма.
 *
 * Здесь только чистые функции: наборы имён, транслитерация и правила Telegram
 * для ников. Проверка занятости и запись в Telegram — снаружи, в ручке: она
 * требует живого соединения, а его нельзя открыть на работающей кампании.
 */

/**
 * Мужские имена и фамилии — по составу купленных партий.
 *
 * Только обыденные имена: редкие запоминаются, а двадцать аккаунтов с редкими
 * именами в одной кампании выглядят как один список, которым они и являются.
 * Поэтому набор имён растёт медленнее набора фамилий — по-настоящему
 * распространённых мужских имён в языке просто меньше, и добивать список
 * архаикой вроде «Пахом» значит менять одну заметность на другую.
 *
 * Повторы внутри партии не страшны: совпадение имён у незнакомых людей никого
 * не удивляет, в отличие от совпадения ников.
 */
const FIRST_NAMES = [
  'Аким', 'Александр', 'Алексей', 'Амир', 'Анатолий', 'Андрей', 'Антон', 'Аркадий', 'Арсений',
  'Артём', 'Артемий', 'Артур', 'Афанасий', 'Богдан', 'Борис', 'Вадим', 'Валентин', 'Валерий',
  'Василий', 'Вениамин', 'Виктор', 'Виталий', 'Владимир', 'Владислав', 'Всеволод', 'Вячеслав',
  'Гавриил', 'Геннадий', 'Георгий', 'Герман', 'Глеб', 'Гордей', 'Григорий', 'Давид', 'Дамир',
  'Даниил', 'Данила', 'Демид', 'Демьян', 'Денис', 'Дмитрий', 'Евгений', 'Егор', 'Елисей',
  'Емельян', 'Ефим', 'Захар', 'Иван', 'Игнат', 'Игорь', 'Илларион', 'Ильдар', 'Илья',
  'Иннокентий', 'Иосиф', 'Камиль', 'Кирилл', 'Клим', 'Константин', 'Кузьма', 'Лев', 'Леонид',
  'Лука', 'Макар', 'Максим', 'Марат', 'Марк', 'Матвей', 'Мирон', 'Мирослав', 'Михаил', 'Назар',
  'Никита', 'Никифор', 'Николай', 'Олег', 'Павел', 'Пётр', 'Платон', 'Прохор', 'Рамиль',
  'Ренат', 'Ринат', 'Родион', 'Роман', 'Ростислав', 'Руслан', 'Рустам', 'Савва', 'Савелий',
  'Святослав', 'Семён', 'Сергей', 'Станислав', 'Степан', 'Тарас', 'Тимофей', 'Тимур', 'Тихон',
  'Трофим', 'Фёдор', 'Филипп', 'Фома', 'Эдуард', 'Эльдар', 'Юлиан', 'Юрий', 'Яков', 'Ян',
  'Ярослав',
];

const LAST_NAMES = [
  'Абрамов', 'Авдеев', 'Агафонов', 'Аксёнов', 'Александров', 'Алексеев', 'Андреев', 'Аникин',
  'Анисимов', 'Антонов', 'Архипов', 'Астахов', 'Афанасьев', 'Бабушкин', 'Баранов', 'Барсуков',
  'Белов', 'Белоусов', 'Беляев', 'Бирюков', 'Блинов', 'Бобров', 'Богданов', 'Бондарев',
  'Борисов', 'Брагин', 'Булатов', 'Бурцев', 'Быков', 'Быстров', 'Вавилов', 'Варламов',
  'Васильев', 'Вдовин', 'Веселов', 'Виноградов', 'Вишневский', 'Власов', 'Волков', 'Воробьёв',
  'Воронин', 'Воронов', 'Гаврилов', 'Герасимов', 'Гладков', 'Глебов', 'Голубев', 'Гончаров',
  'Горбачёв', 'Горбунов', 'Гордеев', 'Горшков', 'Григорьев', 'Гришин', 'Громов', 'Гурский',
  'Гусев', 'Давыдов', 'Данилов', 'Дегтярёв', 'Дементьев', 'Демидов', 'Денисов', 'Дмитриев',
  'Дорофеев', 'Дроздов', 'Дубровин', 'Дьяконов', 'Евдокимов', 'Евсеев', 'Егоров', 'Емельянов',
  'Ерофеев', 'Ефимов', 'Ефремов', 'Жданов', 'Жуков', 'Журавлёв', 'Зайцев', 'Зверев', 'Зимин',
  'Зиновьев', 'Золотарёв', 'Зуев', 'Иванов', 'Игнатьев', 'Ильин', 'Исаев', 'Кабанов',
  'Казаков', 'Калашников', 'Калинин', 'Капустин', 'Карпов', 'Кириллов', 'Киселёв', 'Ковалёв',
  'Кожевников', 'Комаров', 'Кондратьев', 'Константинов', 'Копылов', 'Кораблёв', 'Корнилов',
  'Королёв', 'Костин', 'Котов', 'Кравцов', 'Крылов', 'Кудрявцев', 'Кузнецов', 'Куликов',
  'Курочкин', 'Лаврентьев', 'Лазарев', 'Лапин', 'Ларионов', 'Лебедев', 'Левин', 'Леонов',
  'Лихачёв', 'Логинов', 'Лукин', 'Лыков', 'Львов', 'Макаров', 'Мамонтов', 'Марков', 'Мартынов',
  'Масленников', 'Матвеев', 'Медведев', 'Мельников', 'Меркулов', 'Мешков', 'Миронов',
  'Михайлов', 'Морозов', 'Мухин', 'Некрасов', 'Нестеров', 'Никитин', 'Никифоров', 'Никольский',
  'Новиков', 'Носов', 'Овчинников', 'Одинцов', 'Озеров', 'Орлов', 'Осипов', 'Павлов', 'Панов',
  'Пантелеев', 'Пестов', 'Петров', 'Петухов', 'Платонов', 'Плотников', 'Погодин', 'Поляков',
  'Пономарёв', 'Потапов', 'Прокофьев', 'Пугачёв', 'Рогов', 'Родионов', 'Рожков', 'Романов',
  'Рубцов', 'Румянцев', 'Русаков', 'Рыбаков', 'Рябов', 'Сабуров', 'Савельев', 'Савин',
  'Самойлов', 'Сафонов', 'Сахаров', 'Свиридов', 'Селиванов', 'Семёнов', 'Сергеев', 'Серов',
  'Сидоров', 'Смирнов', 'Соболев', 'Соколов', 'Соловьёв', 'Сорокин', 'Стариков', 'Степанов',
  'Столяров', 'Суворов', 'Судаков', 'Сухарев', 'Сысоев', 'Тарасов', 'Терентьев', 'Тимофеев',
  'Титов', 'Тихомиров', 'Тихонов', 'Токарев', 'Третьяков', 'Трофимов', 'Туманов', 'Тюрин',
  'Уваров', 'Ульянов', 'Устинов', 'Фадеев', 'Фёдоров', 'Филатов', 'Филиппов', 'Фомин',
  'Фролов', 'Харитонов', 'Хомяков', 'Хохлов', 'Цветков', 'Черкасов', 'Чернов', 'Чернышёв',
  'Чистяков', 'Шарапов', 'Шаров', 'Шестаков', 'Шилов', 'Ширяев', 'Шишкин', 'Шмелёв', 'Шубин',
  'Шульгин', 'Щербаков', 'Щукин', 'Юдин', 'Юрьев', 'Яковлев', 'Якушев', 'Ярцев',
];

const TRANSLIT: Record<string, string> = {
  а: 'a', б: 'b', в: 'v', г: 'g', д: 'd', е: 'e', ё: 'e', ж: 'zh', з: 'z',
  и: 'i', й: 'y', к: 'k', л: 'l', м: 'm', н: 'n', о: 'o', п: 'p', р: 'r',
  с: 's', т: 't', у: 'u', ф: 'f', х: 'h', ц: 'ts', ч: 'ch', ш: 'sh',
  щ: 'sch', ъ: '', ы: 'y', ь: '', э: 'e', ю: 'yu', я: 'ya',
};

/** Кириллица → латиница по одному правилу на букву. */
export function translit(value: string): string {
  return (value ?? '')
    .toLowerCase()
    .split('')
    .map((ch) => (ch in TRANSLIT ? TRANSLIT[ch] : ch))
    .join('')
    .replace(/[^a-z0-9_]/g, '');
}

export interface Identity {
  firstName: string;
  lastName: string;
}

function pick<T>(list: T[]): T {
  return list[Math.floor(Math.random() * list.length)];
}

/** Случайные имя и фамилия. */
export function pickIdentity(): Identity {
  return { firstName: pick(FIRST_NAMES), lastName: pick(LAST_NAMES) };
}

/**
 * Ник по правилам Telegram: 5–32 символа, латиница, цифры и `_`, начинается с
 * буквы и не заканчивается подчёркиванием.
 *
 * Обрезаем до 32 ДО удаления хвостового `_`, иначе обрезка сама может оставить
 * его последним символом и вернуть невалидный ник.
 */
export function normalizeUsername(value: string): string {
  let name = (value ?? '').toLowerCase().replace(/[^a-z0-9_]/g, '').replace(/_+/g, '_');
  name = name.replace(/^[^a-z]+/, '').slice(0, 32).replace(/_+$/, '');
  return name;
}

export function isValidUsername(value: string): boolean {
  return /^[a-z][a-z0-9_]{3,30}[a-z0-9]$/.test(value ?? '');
}

/**
 * Второе написание того же имени латиницей.
 *
 * Одно имя пишут по-разному, и это главный ресурс для поиска свободного ника:
 * `alekseyvoronin` занят, а `aleksievoronin` — нет, и выглядит он так же
 * по-человечески. Пока вариантов написания хватает, до цифр дело не доходит.
 */
const TRANSLIT_ALT: Record<string, string> = {
  ...TRANSLIT,
  // «е» намеренно оставлена как есть: замена на «ie» превращала Алексея в
  // alieksiei — вроде и вариант, а читается как опечатка.
  ё: 'yo', й: 'i', ы: 'i', ю: 'iu', я: 'ia', х: 'kh', ц: 'c',
};

function translitAlt(value: string): string {
  return (value ?? '')
    .toLowerCase()
    .split('')
    .map((ch) => (ch in TRANSLIT_ALT ? TRANSLIT_ALT[ch] : ch))
    .join('')
    .replace(/[^a-z0-9_]/g, '');
}

/** Удвоить одну гласную внутри слова: `voronin` → `voroniin`. */
function stretched(value: string): string {
  const positions: number[] = [];
  for (let i = 1; i < value.length - 1; i += 1) {
    if ('aeiou'.includes(value[i])) positions.push(i);
  }
  if (!positions.length) return value;
  const at = positions[Math.floor(Math.random() * positions.length)];
  return value.slice(0, at + 1) + value[at] + value.slice(at + 1);
}

/**
 * Варианты ника от самого «человеческого» к самому случайному.
 *
 * Порядок и есть суть: `aleksey_voronin` выглядит как живой человек, а
 * `alekseyv8421` — как регистрация ради регистрации. Заняты обычно первые,
 * поэтому список длинный, но вырождается постепенно: сначала другое написание
 * того же имени, потом растянутая гласная, и только в конце цифры.
 */
export function usernameCandidates(identity: Identity): string[] {
  const first = translit(identity.firstName);
  const last = translit(identity.lastName);
  const firstAlt = translitAlt(identity.firstName);
  const lastAlt = translitAlt(identity.lastName);
  const raw = [
    `${first}${last}`,
    `${first}_${last}`,
    `${firstAlt}${last}`,
    `${first}${lastAlt}`,
    `${firstAlt}${lastAlt}`,
    `${firstAlt}_${lastAlt}`,
    `${first}${stretched(last)}`,
    `${firstAlt}${stretched(lastAlt)}`,
    `${last}${first}`,
    `${last}_${first}`,
    `${first}_${last}${Math.floor(Math.random() * 90) + 10}`,
    `${first}${last}${Math.floor(Math.random() * 9000) + 1000}`,
  ];
  // Повторы неизбежны: у имени без «й» и «я» оба написания совпадают, а каждая
  // лишняя проверка — отдельный вызов через мобильный прокси.
  return [...new Set(raw.map(normalizeUsername).filter(isValidUsername))];
}

/**
 * Описание профиля.
 *
 * Одна строка про роль и компанию — этого достаточно: длинное описание у
 * человека, который пишет первым, выглядит как реклама. Компания приходит из
 * настроек кампании, потому что аккаунты одной кампании представляют одного
 * клиента.
 */
export function buildBio(company: string | null | undefined): string {
  const name = (company ?? '').trim();
  return name ? `Менеджер компании ${name}` : 'Менеджер';
}

/**
 * Название компании для описания: настройка кампании, а если её нет — имя самой
 * кампании без технического хвоста вроде «-1» или «_Старые аккаунты».
 */
export function companyFromCampaign(
  settingsCompany: string | null | undefined,
  campaignName: string | null | undefined,
): string {
  const explicit = (settingsCompany ?? '').trim();
  if (explicit) return explicit;
  const raw = (campaignName ?? '').trim();
  if (!raw) return '';
  return raw.split(/[_\-–]/)[0].trim();
}
