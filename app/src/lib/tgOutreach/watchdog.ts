/**
 * Сторожевой таймер прогрева: что делать с прогоном, переставшим отчитываться.
 *
 * ЗА КЕМ ОН СЛЕДИТ ТЕПЕРЬ. Только за прогревом. Боевые кампании переехали на
 * единый жизненный цикл задач (app/src/lib/jobs/lifecycle.ts), и их простой
 * ловит порог по колонке прогресса: продления прекращаются, аренда истекает,
 * кампанию подбирает следующий владелец и продолжает с чекпойнта. Прогреву эта
 * замена не подходит — у прогона нет колонки, которая двигалась бы в пределах
 * часов (законная ночная пауза составляет восемь), — поэтому его сторож
 * остался здесь.
 *
 * Лечение идёт по возрастающей:
 *
 *  1. `kill` — просим прогон остановиться и рвём его сокеты. Зависший await на
 *     мёртвом сокете после разрыва падает с ошибкой, и цикл разматывается сам.
 *     Выйдя, тело отпускает аренду, и прогон подбирают заново.
 *  2. `quarantine` — если за отведённое время цикл так и не завершился, значит
 *     он висит там, куда мы дотянуться не можем. Оставляем его висеть, громко
 *     жалуемся и больше не трогаем. Остальная работа продолжается.
 *
 * ТРЕТЬЕЙ СТУПЕНИ (`exit`, падение процесса при общем зависании) БОЛЬШЕ НЕТ.
 * До 05.08.2026 она была единственной, и одна залипшая кампания уносила все
 * остальные: за ночь 14 перезапусков и почти остановленный прогрев ATOL-1.
 * 28.08.2026 её оставили только на случай «работать некому». Под арендой она
 * стала прямо вредной: падение процесса — это чужие аренды. У здоровых
 * соседних кампаний lease_until остаётся живым, никто не подберёт их до конца
 * срока, а перехват потом запишет это как падение и потратит попытку. Ровно
 * то, ради чего аренда и заводилась, ломалось бы одним process.exit.
 *
 * Функция чистая: ни таймеров, ни IO — только решение по снимку состояния.
 */

/**
 * Ручка, которую цикл кампании отдаёт воркеру, чтобы его можно было погасить
 * снаружи. Заполняется циклом сразу после подключения аккаунтов.
 *
 * Одного кооперативного `stop()` мало: он проверяется между шагами, а зависший
 * цикл стоит внутри сетевого await'а и до проверки не доходит. Разрыв сокетов
 * роняет этот await с ошибкой — только так цикл удаётся разбудить.
 */
export interface LoopControl {
  forceDisconnect?: () => Promise<void>;
}

export interface WatchdogSnapshot {
  now: number;
  /** campaignId -> когда кампания последний раз подавала признаки жизни. */
  lastProgressAt: ReadonlyMap<string, number>;
  /** campaignId -> когда мы попросили её умереть (шаг 1). */
  killRequestedAt: ReadonlyMap<string, number>;
  /** Кампании, которые всё ещё числятся запущенными. */
  running: ReadonlySet<string>;
  /**
   * Кампании, уже отправленные в карантин. Нужны, чтобы сторож не планировал
   * им действие каждую минуту: они не оживут, и повторные жалобы только
   * забьют журнал.
   */
  quarantined?: ReadonlySet<string>;
  /** Сколько молчания считать зависанием. */
  stallMs: number;
  /** Сколько ждать после kill, прежде чем сдаться и изолировать кампанию. */
  graceMs: number;
}

export interface WatchdogAction {
  campaignId: string;
  action: 'kill' | 'quarantine';
  /** Сколько прогон молчит, минуты — для лога. */
  stallMin: number;
}

export function planWatchdogActions(snapshot: WatchdogSnapshot): WatchdogAction[] {
  const { now, lastProgressAt, killRequestedAt, running, stallMs, graceMs } = snapshot;
  const quarantined = snapshot.quarantined ?? new Set<string>();
  const actions: WatchdogAction[] = [];

  for (const [campaignId, lastAt] of lastProgressAt) {
    // Уже изолирована — решение принято, повторять его нечего.
    if (quarantined.has(campaignId)) continue;

    const stalled = now - lastAt;
    if (stalled <= stallMs) continue;

    const stallMin = Math.round(stalled / 60_000);
    const killedAt = killRequestedAt.get(campaignId);

    if (killedAt === undefined) {
      actions.push({ campaignId, action: 'kill', stallMin });
      continue;
    }

    // Прогон ушёл из реестра — значит kill сработал, цикл размотался. Слот
    // свободен, а строку подберут по отпущенной аренде; изолировать нечего.
    if (!running.has(campaignId)) continue;

    if (now - killedAt > graceMs) {
      actions.push({ campaignId, action: 'quarantine', stallMin });
    }
  }

  return actions;
}

/**
 * Кого можно забыть: прогон снова отчитывается или уже не запущен, значит
 * запись о попытке убийства больше не нужна и не должна копиться в памяти
 * воркера, живущего сутками.
 *
 * Тем же правилом снимается и карантин: если зависший цикл всё-таки
 * размотался и ушёл из реестра, прогон снова обычный — его подберут по
 * истёкшей аренде.
 */
export function staleKillRequests(snapshot: WatchdogSnapshot): string[] {
  const { now, lastProgressAt, killRequestedAt, running, stallMs } = snapshot;
  const out: string[] = [];

  for (const campaignId of killRequestedAt.keys()) {
    if (!running.has(campaignId)) {
      out.push(campaignId);
      continue;
    }
    const lastAt = lastProgressAt.get(campaignId);
    if (lastAt !== undefined && now - lastAt <= stallMs) out.push(campaignId);
  }

  return out;
}

/*
 * selectOrphanedStartJobs здесь БОЛЬШЕ НЕТ.
 *
 * Она искала команды «старт», зависшие в статусе «выполняется»: команда
 * помечалась завершённой только в `.finally()` цикла кампании, и убитый на
 * середине процесс оставлял её там навсегда, а авто-резюм считал, что старт
 * уже запланирован, и не делал ничего (18.08.2026: пять кампаний простояли 16
 * часов). Причина ушла вместе с механизмом: команда «старт» больше ничего не
 * запускает и закрывается тем же запросом, который её взял, а брошенная
 * кампания определяется истёкшей арендой.
 */
