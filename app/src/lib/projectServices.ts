/**
 * Единый источник истины для списка услуг проекта.
 *
 * До этого SERVICE_OPTIONS дублировался в трёх местах:
 *   - app/projects/new/page.tsx (форма создания проекта)
 *   - app/projects/[id]/page.tsx (карточка проекта)
 *   - components/ProjectList.tsx (таблица/карточка на странице «Проекты»)
 *
 * Когда добавляли новые услуги «AI-звонилка» и «Обучение (аутричу)»,
 * обновили только форму создания, а в карточке и таблице эти кнопки
 * не появились — пользователь не мог отметить их у уже существующего проекта.
 *
 * Все три места теперь импортируют этот массив. Добавление новой услуги
 * — одна правка тут, везде увидится автоматически.
 */
export const SERVICE_OPTIONS = [
  'Аутрич',
  'ТГ аутрич',
  'Лидскан',
  'ЛинкедИн',
  'Перфоманс',
  'Ретаргет',
  'AI-звонилка',
  'Обучение (аутричу)',
] as const;

export type ServiceOption = (typeof SERVICE_OPTIONS)[number];
