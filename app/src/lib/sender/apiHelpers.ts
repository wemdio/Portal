/**
 * Доступ к API инструмента «Рассылка»: только внутренние сотрудники, не демо.
 * Проверка та же, что у остальных инструментов портала — переиспользуем её,
 * чтобы правила доступа жили в одном месте.
 */
export { authenticateRequest, jsonError } from '@/lib/tgOutreach/apiHelpers';
