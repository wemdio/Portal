#!/usr/bin/env node
/**
 * label-replies-rules.mjs — детерминированная разметка исходов ответов (rules-v1).
 *
 * Первый проход бэкфилла reply_outcome_labels: высокоточные regex-паттерны
 * закрывают механически распознаваемые категории (автоответы, отписки,
 * "не туда", ликвидации). Оставшееся размечает Claude батчами (без внешних API —
 * политика проекта). Идемпотентен: уже размеченные пары пропускаются.
 *
 * Точность важнее полноты: паттерн добавляется только если он практически
 * не даёт ложных срабатываний. Сомнительное уходит в LLM-проход.
 *
 * Usage: node label-replies-rules.mjs [--apply]
 */
import { readFileSync } from 'fs';
import { resolve, dirname } from 'path';
import { fileURLToPath } from 'url';
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const { Client } = require('pg');
const __dirname = dirname(fileURLToPath(import.meta.url));
const env = Object.fromEntries(readFileSync(resolve(__dirname, '../../../.env'), 'utf8').split('\n').filter(l=>l.includes('=')&&!l.trim().startsWith('#')).map(l=>{const i=l.indexOf('=');return [l.slice(0,i).trim(),l.slice(i+1).trim()];}));
const db = new Client({ connectionString: env.INSTANTLY_DATASET_DB_URL });
const APPLY = process.argv.includes('--apply');

// Порядок важен: первая сработавшая категория побеждает.
// Паттерны откалиброваны по ~180 живым примерам (демо-кампании Mailganer и др.).
// Приоритет: сначала жёсткие негативы/механика (auto, NDR, wrong, unsub, not_interested),
// затем мягкие. Позитивы (interested/question/referral) НЕ ловим правилами — они требуют
// смысловой оценки и доразмечаются отдельно (риск ложных позитивов слишком высок).
const RULES = [
  // delivery-failure / NDR — самые однозначные
  ['auto_reply', String.raw`(не удалось (выполнить )?доставку|не удалось доставить|адрес(а)? .{0,30}не (существ|найден)|введенный электронный адрес|delivery (has )?(failed|status|failure)|undeliverable|mail delivery|mailbox (unavailable|full)|почтовый ящик (переполнен|вернули|не обслуж)|antispam found|found unwanted object|message you sent .{0,30}(deleted|blocked))`],
  // отпуск/отсутствие (OOO) с диапазонами дат и редиректом на коллег
  ['auto_reply', String.raw`((нахожусь|буду находиться|буду|я)? ?(в|на)? ?(ежегодном |очередном )?отпуск[еа]|в отпуске|отсутству(ю|ет|вать).{0,40}(на рабочем месте|в офисе|до|без доступа)|буду отсутствовать|отсутствие на рабочем|без доступа к (почте|телефону|электронной)|временно (недоступен|отсутству)|on (annual )?(leave|vacation)|out of (the )?office|i am (currently )?(out|away|on)|с \d{1,2}[.\s/]\d{1,2}.{0,25}(по|до).{0,25}\d{1,2}[.\s/]\d{1,2}.{0,40}(отпуск|отсутств|вернусь)|по всем (вопросам|неотложным).{0,30}(обращайтесь|прошу обращаться)|по срочным вопросам.{0,30}(пишите|звоните|обращ|whats|телеграм))`],
  // смена/неактуальность адреса электронной почты
  ['auto_reply', String.raw`((адрес|почт[аеу]|ящик|email|e-mail).{0,45}(измен(ён|ен|ил)|замен(ён|ен)|больше не (использ|действ|актуал)|не (используется|актуал(ен|ьна|ьн)|действ)|прекра(тил|щает)|переход[еа]? на)|(новый|актуальн(ый|ая)|обновлённ(ый|ая)).{0,15}(официальный )?(адрес|email|e-mail|почт)|просим .{0,25}(направлять|отправлять|присылать).{0,30}(на (новый|адрес|почт|обновл)|по (новому|адресу))|смен(а|е|ился|илась) (адреса|почты|реквизит)|уведомля(ем|ю).{0,40}смен)`],
  // ticketing / автоприём (рус + англ)
  ['auto_reply', String.raw`(ticket #|request \(?\d{3,}|has been (created|received)|your (request|ticket|message|inquiry).{0,30}(received|created|registered)|we('| ha)?ve received your|thank you for (contacting|reaching out|your (email|message))|we will (respond|get back)|please type your reply|gdpr request|номер (запроса|вашего запроса|вашей заявки|тикета)|присвоен.{0,10}номер|ваш[аеи].{0,25}(заявк|обращени|запрос).{0,35}(создан|зарегистр|принят|получен|в работ|номер)|спасибо,? получил|получено[.!]?\s*$|спасибо,? (мы )?(ваше письмо )?получили|принято к сведению|принято в работу)`],
  // ticketing / автоприём обращений / «свяжемся» / OOO / гос-уведомления
  ['auto_reply', String.raw`(автоматическ(ий|ое|и) (ответ|уведомлен|сформирован|создан)|это автоответ|автоответчик|out of office|ваше (письмо|сообщение|обращение|заявка|запрос) (получен|принят|зарегистрир|доставлен|зафиксирован)|мы получили ваш|получили ваше (письмо|сообщение|обращение)|заявк[аи] (зарегистрир|принят|получен|присвоен|успешно)|ваш[аеи]? (заявк|обращени|запрос).{0,40}(зарегистрир|принят|получен|в работ|номер)|номер (вашего )?(обращения|заявки)|присвоен номер|ваш[уе].{0,15}(заявк|обращени).{0,40}номер|специалист[ы]?.{0,30}свяж|менеджер.{0,30}свяж[ие]|эксперт[ы]?.{0,30}(свяж|выйдут на связь)|коллеги.{0,20}свяжутся|с вами свяжет?ся|ответим (вам )?(в течение|в ближайш|не позднее|после)|не отвечайте на (это|данное|эт)|сообщение (создано|сформировано|сгенерировано) автоматически|нахожусь (в отпуске|вне офиса|без доступа)|в отпуске (до|по|с)|отпуске? до \d|вернусь \d|на больничн|в командировке|в декретн|декретном отпуске|без доступа к почте|дежурн(ый|ая) менеджер|обрабатываем.{0,20}в порядке очереди|в порядке очереди|поздравляем.{0,25}(с наступающ|праздник|новым годом)|график работы (в период|на период|изменён)|обращения.{0,40}не рассматрива|воспользуйтесь (интернет-приемной|сервисом|порталом)|оцените (качество|ответ|мою работу|работу)|данный (почтовый )?(адрес|ящик) (не использ|больше не|вернули)|сменил(ся|ась)? (адрес|почта)|новый (адрес|email)[:\s]|направьте.{0,20}на (наш|нашу|следующ)|просьба не отвечать)`],
  ['unsubscribe', String.raw`(прекратите (мне |нам )?(писать|слать|спамить|рассыл)|перестаньте (писать|слать|беспокоить)|хватит (писать|слать|спамить)|удалите (нас|меня|наш|мой|нашу почту|мой адрес|из (базы|рассылки|списка))|исключите (нас|меня|наш адрес|мой адрес) из|отпишите (нас|меня)|уберите (нас|меня|наш|мой) из|не пишите (нам|мне|сюда) (больше|более)|больше (нам |мне )?не пишите|^\s*спам\s*$|это спам[ !.]|пометил.{0,12}как спам|не беспокоить|просьба не беспокоить|пожалуюсь|жалоб[ауы] в (роскомнадзор|фас)|внесем в спам)`],
  ['wrong_person', String.raw`((я |уже |давно )?(здесь |тут |там )?(больше )?не работаю|не работаю (в|здесь|тут|у)|сотрудником .{0,40}не являюсь|не являюсь (сотрудником|работником)|уволил(ся|ась|и)|вы ошиблись( адресом| почтой| номером)?|ошиблись адресом|(письмо|обращение).{0,20}(не нам|не по адресу|надо направить|не тот)|не по адресу|попали не туда|это (личная|частная|моя личная) почта|мы не (та компания|та организация|занимаемся (этим|таким)|медицинская|клиника)|проект заморожен|не осуществляем деятельность|организация ликвидирована|ликвидирован[ао]? (с|в|\d|19|20)|компания (закрыта|закрылась|ликвидирована|прекратила)|мы закрылись|предприятие (закрыто|ликвидировано)|фирма (закрыта|ликвидирована)|(организация|компания|мы) банкрот|признан[ао]? банкротом|ceased operations|no longer (active|work))`],
  ['not_interested', String.raw`(^((добрый день|здравствуйте|спасибо|благодарю)[!,.\s]{0,6})*(нам |мне |нас )?(это |этот |ваш[еи]? )?(не ?интересн|не интересует|не актуальн|не требуется|не нужн|не надо|неинтересно|не рассматрива|не планир)|спасибо,? (но )?(нам |мне |это )?(не интересн|не актуальн|не нужн|не требуется|не рассматрива|не планир|отказ)|не заинтересован|нет потребности|не нуждаемся|нет (необходимости|надобности)|услуги (не требуются|не нужны|нам не)|не рассматриваем (сотрудничеств|новый|ваш|предложен)|не планируем (такую|менять|переход|сотруднич)|отказываем|нам не нужн|у нас (уже )?(всё|все) (есть|устраивает|автоматизир)|нас (всё|все) устраивает|работаем с другим|есть (действующий |свой )?(подрядчик|провайдер|сервис|поставщик))`],
];

(async () => {
  await db.connect();
  let totalMatched = 0;
  for (const [label, pattern] of RULES) {
    // best-body на пару (как в LLM-проходе): самое длинное тело
    const sql = `
      WITH best AS (
        SELECT DISTINCT ON (campaign_id, lead_id)
          campaign_id, lead_id,
          lower(trim(regexp_replace(coalesce(body_text,''), '[[:space:]]+', ' ', 'g'))) AS body,
          md5(coalesce(body_text,'')) AS body_hash
        FROM raw_emails
        WHERE ue_type = 2 AND lead_id IS NOT NULL AND campaign_id IS NOT NULL
          AND timestamp_email BETWEEN '2025-07-01' AND now() + interval '1 day'
        ORDER BY campaign_id, lead_id, length(coalesce(body_text,'')) DESC
      )
      ${APPLY
        ? `INSERT INTO reply_outcome_labels (campaign_id, lead_id, label, confidence, model, rubric_version, body_hash)
           SELECT b.campaign_id, b.lead_id, '${label}', NULL, 'rules-v1', 'v1', b.body_hash FROM best b
           LEFT JOIN reply_outcome_labels l ON l.campaign_id = b.campaign_id AND l.lead_id = b.lead_id
           WHERE l.campaign_id IS NULL AND b.body ~ $$${pattern}$$
           ON CONFLICT DO NOTHING`
        : `SELECT count(*) FROM best b
           LEFT JOIN reply_outcome_labels l ON l.campaign_id = b.campaign_id AND l.lead_id = b.lead_id
           WHERE l.campaign_id IS NULL AND b.body ~ $$${pattern}$$`}`;
    const r = await db.query(sql);
    const n = APPLY ? r.rowCount : Number(r.rows[0].count);
    totalMatched += n;
    console.log(`${label.padEnd(16)} ${APPLY ? 'inserted' : 'would match'}: ${n}`);
  }
  console.log(`TOTAL: ${totalMatched}${APPLY ? '' : '  (dry run — --apply to write)'}`);
  await db.end();
})().catch(e => { console.error('FAIL:', e.message); process.exit(1); });
